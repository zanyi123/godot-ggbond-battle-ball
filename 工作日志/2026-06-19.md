# 决竞球 Battle Ball - 工作日志

## Tips（置顶）
- 每次对话写入当日工作日志
- 记得更新前一天的任务状态

---

## 2026-06-19

### 今日完成

**3D 素材导入咨询（未动手，已讲清边界）**
- 梳理 3D 工作流程：决定路线→备素材→替换节点→AnimationTree→接线→模拟验证
- 明确 zcode 能力边界：能做接线/状态机/验证，做不了建模/贴图/K帧/绑骨（需美术或 Mixamo/Cascadeur 外部工具）
- 素材形态决定工作量：带动画素材(Mixamo)→动画不用另做；裸模→动画要外部工具另做
- 给出 4 条路线建议（真3D/2.5D/精灵帧/占位），主人定方案后再开干

**Bug 修复：球员死贴发球线抖动抢球**
- 根因：CHASE_BALL/GOTO_BALL 状态缺卡死检测，球在对方半场时 _clamp_to_half_field 把目标压到中线，球员贴线够不到球→目标 y 每帧随球跳变→抖动
- 改 ai_manager.gd 4 处：
  - ap 字典加 chase_cooldown 字段（追球冷却锁）
  - _physics_process 每帧递减 chase_cooldown
  - _decide 两处追球入口检查冷却，冷却内不进 CHASE_BALL
  - CHASE_BALL 分支重写：卡死检测 + 严格放弃条件（贴线且球真实在对方半场才放弃）+ y 轴锁定（贴中线追球锁 y 抑制抖动）
- 验证：4 轮迭代（42→14→2→0 卡死），最终 3 场危险数=0 硬规则全通过，传球率 100%

**方法论收获**
- bug 修复「卡死」类问题不能只补检测，要分清是真卡死还是抖动（画面信息很关键，主人一句"抖动为主"直接纠正方向）
- 即时放弃逻辑要严格：不能只看"贴边界"就放弃，要叠加"球真实在对方半场"才放弃，否则误判正常追球加剧抖动
- 目标位每帧重算是抖动元凶，贴墙追球锁单轴能有效抑制

### 待完成任务
- [ ] 主人开游戏手动验证：球员贴发球线是否还抖动（数据测不到的视觉手感）
- [ ] 3D 素材导入方案待主人定路线后开干
- [ ] seed=3 状态切换偏高(224)可选优化（需位置缓存机制，即 P2 前置依赖）
- [ ] P2 影响力地图接入站位（需先做位置缓存机制）

### ⚠ 遗留问题：菲菲压发球线（夜间 zcode 会话，未彻底修复）

**背景**：6.19 日间 pi_对话 会话做的 chase_cooldown+y轴锁定 修复**未提交，随会话结束丢失**。夜间 zcode 会话在基线 a1269d4（不含该修复）上重做，但主人实测仍压线。

**夜间尝试过的方案（均未达标）**：
1. v1 `enemy_too_deep` 阈值60：阈值设错，敌人 x 才 10-32 到不了 60，从未生效
2. v2 clamp 截断就不冲抢：过严，supporter 全退守堵门 → seed=2 退化为 0-0 僵死
3. v3 CHASE 超时退守 + 冲抢冷却：引入编译错误（current_state 跨函数作用域）→ AI 全瘫；修复后又状态震荡 → 卡死 113
4. 极简方案（当前保留）：CHASE_BALL 追到目标但球被敌人拿着 → 转 DEFEND 回阵型位（ai_manager.gd:1605-1608）。模拟 seed3/4/5 全绿卡死0，但**主人实测仍压线**

**根因诊断（已确认）**：
- 菲菲 supporter + speed=85 最快 + 高 aggro_range → 总满足冲抢条件
- 敌人持球过中线，clamp 把追球目标压到中线 -10，物理上够不到对方半场的敌人
- 每 think_interval 重决策又追 → 磁铁吸发球线

**未彻底修复的可能原因**（待下轮排查）：
- 极简方案只在"到达目标"时判断，但菲菲可能在到达前的移动途中就压线（速度太快，arrive 阈值15px，她1帧就穿过去又回来）
- 或菲菲从飞行/落地分支进 CHASE_BALL，球权变化时状态未及时重置
- run_sim 验证通过 ≠ 功能正常（模拟里敌人半场逗留场景可能触发不到该 bug）

**教训**：
- 在状态机决策端层层打补丁是错的（v1/v2/v3 每个都引入新震荡路径）
- stash 操作前必须确认工作区状态，本次 stash 把技能UI改动一起卷走，靠 git fsck 找回
- seed=1 是不稳定种子（基线也卡死 0-81 波动），验证应换稳定种子

**当前代码状态**：ai_manager.gd 保留极简方案（7行），技能UI改动已找回。菲菲压线留作下轮专题。

### 下一步方向
- 等主人验证贴发球线抖动修复的视觉手感
- 等主人定 3D 导入路线（真3D/2.5D/精灵帧/占位）
- 可选：位置缓存机制（同时服务 P2 接入 + 降低状态切换抖动）

---
