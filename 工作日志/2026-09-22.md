# 2026-09-22 工作日志 — base_cup 挂点系统上线 + low_gu 合规线尝试与挂起

## 今日目标

- base_cup（菲菲 base 系手型版）④球挂点校准上线
- low_gu_v2 合规线批量尝试（后经主人纠偏转向）

---

## 完成内容

### 1. base_cup ④球挂点系统上线 ✅（主人验收：球贴右手）

**文件**：`scripts/battle3d/visual/player_proxy_3d.gd`、`scripts/battle3d/battle3d_const.gd`、`scripts/battle3d/battle_arena_3d_bridge.gd`

- **运行时顶点云自动贴合**：`_build_cup_anchor()` 构建时扫描网格顶点解算右手杯腔质心 → `_physics_process` 每帧把 HandProxy 贴到实测杯心（几何事实，与朝向/缩放无关）
- **骨骼蒙皮定位路径**：骨架模型（low_gu 系）优先按 `skeleton.find_bone("RightHand")` + ARRAY_BONES/WEIGHTS 权重筛选（尺度无关），base 系无骨架走几何筛选回退
- **球模型缩小**：BALL_SCALE 30→11.75（原生直径实测 1.1927，35.8px→14px=身高18%，主人拍板）
- **实时调试系统**：bridge `[挂点调试]` 每秒打印 hand/cup_real/ball 三方全局坐标（换装校准标准工具）
- 排障实录：巨球遮蔽误判→投影屏幕轨迹分析→世界坐标实时打印闭环，最终几何贴合一次修准

### 2. low_gu_v2 合规线尝试（尝试后挂起，等素材源修复）

- ①拓扑修复 ×7（p1/3~8）：非流形边清除率 97.7%~99.0%
- ②杯型手 p1/3/4 完成（批量工艺：朝向目检→自动腕线→45°卷+15%内收→球体验收→导出）
- p2 low_gu_v2 手型版：RightHand 权重定位+保蒙皮杯型；**Jog 动画挂载验证通过**（轨道重定向 mixamorig:→原名 330 条全生效，蒙皮跑姿零撕裂——③核心风险排除）
- **挂起原因**：混元绑骨骨骼尾端数据异常（Head 60.5cm / root 39.1cm / 双臂 18+，清单在案），GLB/FBX 双管线进 Godot 均章鱼；主人判定素材源头问题
- 产物全部归位 `建模素材库/.../player2/修复/`，游戏零引用（assets/game_models 已清理）

---

## 方向修正（主人纠偏，重要）

- base 系批量（p1/3/4）非主人本意：合规素材=low_gu_v2 系且只有 p2 备齐；base 系产物全部搁置
- **换装三关卡机制确立**：①Blender 主人目检 → ②Godot 导入预览判定（主人演示的低成本高可靠管道）→ ③主人拍板才进 game_models+MODEL_MAP
- 教训：未验收模型禁止直接替换主游戏已验收内容

---

## 踩坑记录

- Blender 5.1 API：`Action.fcurves` 移除（slotted：layers→strips→channelbags→fcurves）；`export_use_selection`→`use_selection`；`nla_tracks` 无 clear（逐个 remove）；`action_slot=None` 在无 action 时报错；Object 无 `update()`
- Godot 4.6 运行时无顶点组 API——蒙皮筛选用 `ARRAY_BONES/ARRAY_WEIGHTS` + `skeleton.find_bone`
- `render.opengl` 依赖窗口前台重绘 + 场景 Camera 会抢占渲染视角（删场景相机回退视口视角）；后台化窗口渲染=空缓冲
- **EEVEE 离线渲染**（配相机+太阳灯）= 不依赖窗口前台的可靠出图管道 ✅
- `primitive_uv_sphere_add(location=)` 吃**世界坐标**（喂局部埋地下）
- `_build_cup_anchor` 几何筛选尺度敏感（base 米制 vs low_gu 厘米制）→ 已升级骨骼蒙皮路径
- FBX 系模型（low_gu_v2）绑定姿态为垂臂站姿（非 T-pose），掌心朝向需 PCA 判定

---

## 待办 / 下一步

- [ ] ③共有动作导入（base 线纵向推进）：载体路线待主人拍板（low_walk 改造 vs base 现场绑骨）
- [ ] ④挂点骨架版：动画下杯心跟手骨
- [ ] low_gu_v2 线挂起备案：骨骼 tail 异常清单已量化（Head 60.5/root 39.1），等素材源修复后复用成熟工艺
- [ ] p1/3/4 base 系产物搁置中（各自 修复/ 目录，主人可随时删）
- [ ] low_gu_v2_cup.glb 含 42 顶点棱角球残留（历史谜案，无害）

## 预算

- 本会话累计消耗约 5500（Blender MCP 大头：批量杯型手+验证渲染），预算已放开（主人指示"预算充足"）


---

## 实施窗口追加（同日深夜）：13 拆分执行 + #1 复制系 + 波6 收官整改

> 本节为债务/实施窗口（同一工作区）追加，与上方模型窗口记录并行不冲突。

### 1. 13 拆分执行（纯等价重构，步骤1-5）✅
- handler 1783 行单文件 → 继承链四文件+facade：`handler/base_route.gd`(323) ← ball_route(282) ← player_route(323) ← field_route(301) ← `spirit_tag_effect_handler.gd`(558)
- 实现机制 = GDScript extends 继承链（四文件拼一类，状态/方法同实例解析）——比子节点组合等价性更强（零引用重连）；对外类名/方法签名/信号 100% 不变
- 两处链式限制发现并修正：①signal 声明必须在链底 base；②基类静态调子层方法编译失败 → 分发处 `_apply_*` 全部 call() 动态化 + 兄弟层调用动态化 + pending 声明统一上移 base
- 等价性验证：语法四文件 OK + 全量回归全绿 + run_sim 3 场硬规则 3/3 危险=0

### 2. #1 复制系叠加（步骤6）✅
- trigger 历史环形缓冲 20（_record_cast 于 _fire_skill 成功尾部，主动/被动都记；复制系标签不入史防自噬）
- 暗黑共享槽（put/take_shared_copy + _clock 过期）；SKILL_COPIED 事件（event_bus 新枚举，梦幻束缚反制钩子）
- skill_copy_last（复制最近敌方释放、能耗×cost_pct 折扣、无快照 fallback 失败）+ skill_share_copy（全队可复制槽）
- handler `_get_trigger` 链式查找 + `trigger_ref` 显式注入口（测试用）

### 3. 验收整改（验收窗口独立复核后按 skill 返工）✅
- **P1 迷雾敌我不分返工**：FieldEffectZone 加 caster_team；manager get_perception_scale_at 加 viewer_team 分队；ai_manager 传 ap.team；wave6_finale 18/18（补雾内敌方0.4/雾内本方1.0/雾外1.0 三用例）
- **拆分中间态澄清**：验收窗口"编译断裂"快照系撞上拆分初版（19:29 有错，20:0x 修复后全绿）；本轮实证当前树健康（test_pipeline/test_passive 场景复跑 PASS）
- **sim 偶发第4次**：seed3 卡死3，三遍复跑全 0 不复现（历次同模式）

### 4. 排产缺口上报 ⚠
- #6 地形/削能墙从未排进任何波次（验收发现）→ 主人已令按《16_波7工单》补遗实施（见后续追加）
- 08/02 账面滞后（规划窗口整文件重写覆盖实施窗口回写）已修正；教训=共享账本 replace 必须带 assert+写后 grep 复验


### 波7 补遗收官：#6b 削能墙 + #6a 地形改动（按 16 工单，2026-09-22 深夜）

- **三项附带整改先行**：①补实施工作日志（本文件上节）②13→15 文件更名+08 引用同步 ③skill_copy_last 参数 damage_pct→cost_pct（registry/handler/测试三处同步，行为不变）
- **#6b 削能墙**：`drain_wall.gd` 障碍子类（extends Obstacle）——捕获半径内球被吸向墙心（move_toward）+ 走既有 stuck 逐帧削攻/速口径；不做击穿（consume_frame 空）；吸住超时释放继续飞；耗尽走既有 _stop_and_return 球权。manager ball_ref 注入链（battle_manager→obstacle_manager→create_obstacle set 转发）
- **#6a 地形改动**：field_physics_manager 加 terra_zones + add_terra_zone + get_friction_at 分区查询（多重取最滑）+ Timer 到期清理；handler field_terra_change 空壳转正（radius>0 区域模式 / radius=0 全场 set_friction 立即可用）
- **超清单上报（R3）**：#6a 完整区域消费需 player._get_field_friction 传位置（一行接线），player.gd 不在 16 工单清单未动——当前 get_friction() 全场语义行为等价，接口就绪待批接线
- **验收**：test_wave7_finale **12/12** + 全量回归全绿（三件套+10 项 headless+mults，累计 170+ 断言）+ run_sim 3 场一次通过危险=0
- **里程碑**：**原语台账 24 条目全绿，原语阶段正式收官**。registry 102 标签。技能产出全部交由主人在元灵管理（快捷设置系统）组合标签完成


---

## 提交记录（实施窗口，2026-09-22）

| 提交 | 内容 |
|---|---|
| `f2057d0` feat | #1 复制系落地（历史缓冲/暗黑共享/SKILL_COPIED）+ P1 迷雾敌我区分返工 + 波7 补遗（#6b 削能墙 + #6a 地形改动）——原语 23+2 全落地，registry 102 |
| `0f1a15c` docs | 原语收官账本（02 全 24 项 ✅ 含 #6 拆 6a/6b、08 波7 行 23/23、12/15 执行记录、16 工单）+ P1 迷雾整改记录 + 工作日志 |

> 07 排产表（已作废）由规划窗口删除，随本批入库终态。`.zcode/config.json` 本机配置继续排除。
> 推送状态：本地三提交待推（GitHub TLS 通道间歇不可达，网络恢复后 `git push origin main` 即可）。
