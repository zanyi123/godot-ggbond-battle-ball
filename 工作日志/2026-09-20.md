# 2026-09-20 工作日志 — P0-1 双扣能修复 + P2-1 ball_mods 隔离（zcode · 债务窗口）

> 承接 `docs/待修问题提示词.md` 债务清单（9-17 调研产出）。本日（跨夜会话）完成两项：P0-1 验证属实并修复、P2-1 按 04 计划四步全量执行。

## 一、P0-1 玩家技能双扣能量+双CD表：验证属实 → 修复 ✅

- **验证**（静态追踪全链路）：玩家路径 `player.use_skill` 扣能设CD → 信号 → battle_manager → spirit_system → trigger `_fire_skill` 再扣一次能、再设一张CD表（双重把关）；AI/被动只经 trigger 扣一份。9 个主动技能 energy_cost 全非零，双扣真实发生。
- **修复（方案 A，主人拍板：player 只做资格前置，扣能与CD全权归 trigger）**：
  - `player.gd`：`use_skill` 删扣能/设CD（保留沉默/解锁/能量预检查）；CD判定与 `get_skill_cooldown_ratio`（HUD用）转发 trigger 权威表，无注入时回退本地表
  - `spirit_skill_trigger.gd`：成为全路径唯一扣费/CD点；扣费=(基础+标签附加)×施法者消耗倍率，CD×施法者CD倍率；新增 `get_skill_cooldown_ratio` / `add_player_skill`（增量名册接口）
  - `battle_manager.gd`：三条初始化路径注入 `player.spirit_trigger`；开赛时 manual 天赋技能增量同步进 trigger 名册（顺带修好"天赋树解锁技能被 trigger 名册拒绝放不出来"）
- **主人纠偏两处**（已核实采纳）：①消耗/CD折扣倍率不是天赋，是技能标签效果写入球员的数据（handler:1141/1149）——衔接=trigger 扣费/设CD时读施法者倍率，两行搞定；②talent_system:169 是 manual 注入的CD初始化，非"CD重置天赋"
- **验证**：gdscript_check 无新增问题；test_skill_mults 22/22；run_sim 3 场危险=0

## 二、P2-1 ball_mods 隔离：按 `技能规划/04` 四步全量执行 ✅

- **Step 0 调用链梳理**（图已贴 04 文档）：生产三路径（玩家/AI/sim）attacker 均可靠；测试路径有 attacker=null（快照默认即等价）；旧体系 `_apply_ball_skill` 与新体系零交叉
- **Step 1 球侧快照化（纯等价重构）**：ball 新增私有 `ball_mods` 快照，launch 时经 handler 新公开只读接口 `take_ball_mods_snapshot` 领取；全部消费点（追踪/回旋/直行/AOE/穿透/伤害速度/lockon/弹道豁免）改读快照；回旋触发状态内联为球私有；删 5 处直写 + SpiritTagEffectHandler/SpiritSystemManager 类型直引（grep 归零）。验证：回归三测试 PASS + run_sim 危险=0
- **Step 2 按投球者隔离+有效期（行为修复）**：
  - handler `_ball_mods` → `_ball_mods_by_caster`（每人一张纸，修串味）；写入带 `expires_at`（handler 自累计比赛时钟 `_match_clock`，Engine.time_scale/暂停自动同步——修无期限）；投球取走即清+过期字段回落默认值（修覆盖残留）
  - trigger `_fire_skill` 只清施法者自己准备区（`reset_ball_mods_for`，退役全清）
  - 旧 getter 走"最近活跃 caster"兼容视图 → ai_manager / field_tag_test_panel 零改动
- **Step 3 回写**：01 缺口台账 #1 ✅、02 §七批次记录、04 执行记录全勾、待修提示词 P2-1 标"第一轮完成"、本日志
- **验证**：新增 `scripts/test3d/regression/test_ball_mods_isolation.gd` **9/9 PASS**（隔离/过期/取走即清/兼容视图/清扫）；test_ballistic_ball / test_air_throw / test_fp_mode 全 PASS；test_skill_mults 22/22；run_sim 3 场危险=0；seed1 复跑确认波动为 AI 随机分叉（同 seed 两场 3-2/34击中/149切换 vs 3-1/13击中/133切换，均危险=0）

## 改动文件清单

| 类别 | 文件 |
|---|---|
| 生产代码 | `player.gd`、`spirit_skill_trigger.gd`、`battle_manager.gd`（P0-1）；`ball.gd`、`spirit_tag_effect_handler.gd`（P2-1；trigger 交叉一处） |
| 新增测试 | `scripts/test3d/regression/test_ball_mods_isolation.gd` |
| 文档回写 | `技能规划/01`（台账#1✅）、`技能规划/02`（批次记录）、`技能规划/04`（执行记录+调用链图）、`docs/待修问题提示词.md`（P0-1、P2-1 均已标注完成）、本日志 |

## 待办 / 下一步

- [ ] **P0-2（AOE 范围伤害落空）**：下一个债务，在 ball.gd，建议下轮做（本轮已错开）
- [ ] P2-1 第二轮（可选）：ball.launch 组查找改 player 侧注入快照，彻底解耦
- [ ] 观察项：`ai_manager.gd:1279` 旧AI读 `player.skill_cooldowns`（P0-1 后该表恒 0 不再写入）——sim 无危险，若 AI 决策异常优先查这里；`talent_system.gd` 兜底写 player 表同理（无害）
- [ ] 已知噪声：sim 同 seed 方差大（AI randf 分叉），切换数偶超警告阈值（133/149），两次复跑均危险=0，非系统性回归
- [ ] `run_tests.sh`（test_ai_utils）在本机 Git Bash 卡死（-s 模式历史问题，与本轮改动无关）；test_skill_mults 同模式可跑已替代

## 三、P0-2 AOE 范围伤害落空：验证属实 → 修复 ✅（同日续，主人拍板）

- **验证**：生产代码仅 `illusion.gd:94`（幻象）注册 "players" 组，player.gd 零注册；ball AOE 遍历该组又显式跳过幻象 → 命中恒空为逻辑必然，所有带 `ball_range_up` 标签的技能静默失效
- **修复**（ball.gd 单文件，主人确认方案后执行）：
  - AOE 索敌改 `_collect_aoe_targets()`：数据源 = `_get_all_players_array()`（`GameManager.team_a/b` 权威名册，battle_manager 两条创建路径赋值，与 handler/trigger 注入名册同源）——**三处索敌口径统一**（P2-7 的主要诉求顺势达成）
  - `_get_all_players_array()` 加兜底：名册空时回退组遍历（保 4 个自注册组的测试场景；生产走不到）
  - 幻象维持"不吃 AOE"设计现状（过滤条保留为纯防御+意图声明，是否让幻象吃 AOE 留主人日后裁决）
- **验证**：新增 `scripts/test3d/regression/test_aoe_targets.gd` **7/7 PASS**（敌队存活入选/本人/超距/被击败/隐身排除/半径边界）；回归三测试 PASS；隔离测试 9/9 复跑 PASS；run_sim 3 场危险=0
- **经验**：`-s` 模式入口脚本编译期与运行早期都拿不到 autoload（test_skill_mults 的 autoload print 出现在 quit 后佐证）——测试需要 autoload 数据时改为注入 source 参数做纯逻辑断言，接线靠 grep+sim 集成覆盖

## 四、P1-1 组协议失效三连：验证属实 → 修复 ✅（同日续三，主人确认）

- **验证**：①"players"组已随 P0-2 闭环；②"ball"组生产零注册（仅 3 个测试注册）→ `player._get_ball_node` 恒 null → **球技能光环永不显示**；③ball 死连接 "spirit_system" 组的 `skill_activated/cancelled`（组内只有 handler 无此信号，has_signal 防御静默跳过）——信号真实属于 SkillStateManager（input_manager 创建、真实 emit）
- **根因判定**：②③是同一功能（球技能光环）的两条实现路径，两条都断；收敛为单一路径
- **修复**（3 文件）：player.gd 加 `ball_ref` 注入字段 + `_get_ball_node` 优先注入（组查询兜底）；battle_manager.gd 两条初始化路径注入（与 spirit_trigger 同点同模式）；ball.gd 删 4 个死路径函数 + 孤儿 `_get_skill_data`（连带消掉 P2-2 的一个"每次IO"点）
- **sim 危险项排查（重点）**：首轮 sim seed2 出现偶发极端局（卡死10/击中267/39.5秒拉锯）→ 复跑 seed2 ×2 不复现 → **stash 对照实验**：回退三文件（含 P0-1/P0-2 改动）后 seed2 **三遍确定性僵死**（0-0零击中，完全一致）——证明是此前修复让 seed2 脱离僵死分支，极端局为偶发噪声，P1-1 排除嫌疑 → 新代码两轮全量 6 场全部危险=0、卡死全 0
- **验证**：语法无新增问题；回归三测试 PASS；隔离 9/9、AOE 7/7 复跑 PASS；grep 断言死路径零残留
- **待主人目检**（headless 覆盖不了输入）：编辑器进游戏，持球按数字键激活技能→球亮光环；C 键取消→熄灭；球权转移→光环正确消失
- **新发现**：`ai_manager._eval_ball_threat` 读 `ball.active_skill_data`（球带技能→威胁+0.3）——光环路径修复后该字段在持球激活期首次有值，AI 对带技能球更警觉，属设计预期；若后续 AI 行为异常优先查这里

## 五、提交记录

- 本日四轮修复（P0-1 / P2-1 第一轮 / P0-2 / P1-1）+ 验收测试 ×2 + 债务提示词标注 + 本日志，已提交 GitHub（origin/main）
- 同批入库：新系统窗口产出（技能规划 01~04、美术规划 01~02、原作资料填写版、标签整合清单迁移、动作探针）与模型窗口改动（battle3d 桥接、量具）
- `.zcode/config.json`（本机 MCP 路径）不入库

## 六、债务总账（截至本日）

| 项 | 状态 |
|---|---|
| P0-1 双扣能+双CD表 | ✅ |
| P0-2 AOE 落空 | ✅ |
| P1-1 组协议三连 | ✅ |
| P1-2 事件总线 | ⬜ 下一个 |
| P2-1 球与技能边界 | ✅ 第一轮（第二轮解耦注入链可选） |
| P2-2~P2-7 | ⬜（P2-2 已顺带消一个 IO 点、P2-7 已顺带统一口径） |
| P3-1 handler 重构 | ⬜ 最后 |
