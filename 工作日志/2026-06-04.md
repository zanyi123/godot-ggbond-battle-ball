# 决竞球 Battle Ball - 工作日志

## Tips（置顶）
- 每次对话写入当日工作日志
- 记得更新前一天的任务状态

---

## 2026-06-04

### 今日完成

#### 一、元灵技能标签系统架构搭建

**三大分类**：对球(BALL)、对场地(FIELD)、对球员(PLAYER)

**核心组件**：
1. 标签注册表 (`tags_registry.json`) — 标签元数据
2. 技能触发器 (`spirit_skill_trigger.gd`) — 读取技能JSON→提取标签和数值→调用效果函数
3. 标签效果处理器 (`spirit_tag_effect_handler.gd`) — 执行标签效果，管理球修饰符
4. 系统管理器 (`spirit_system_manager.gd`) — 统一入口

#### 二、快捷设置系统（开发者工具）

- 球员管理面板：左侧列表+右侧属性滑块+新建球员
- 元灵管理面板：左侧列表+右侧详情+技能编辑弹窗（标签按钮选择）
- 数据同步器：读写JSON、自动生成ID、模板创建
- 主菜单接入绿色"快捷设置（开发者）"按钮

#### 三、标签精简重整

**删除6个不切实际标签**：护甲(应对球员)、移动障碍、固定障碍、分身、分身清除、解除缴械

**范围扩大重定义**：从"改飞行距离"改为"球命中敌人时AOE范围伤害"

**精准锁定实现**：发球时自动瞄准最近敌人方向

#### 四、标签拆分：百分比/固定值独立

**原则**：每个标签只做一件事，不再用value_type切换

原来"增伤"一个标签内同时支持百分比和固定值 → 拆成"增伤(%)"和"增伤(固定)"两个独立标签

**拆分明细**：
- BALL：增伤/减伤/加速/减速 各拆2个 → +4
- PLAYER属性：攻/防/速/韧各± 各拆2个 → +8
- PLAYER体力：恢复/掉血 各拆2个 → +2
- PLAYER能量：恢复/消耗/最大± 各拆2个 → +4

**最终标签数：80个**（BALL 17 + FIELD 12 + PLAYER 51）

#### 五、对球(BALL)标签效果全部实现（17个）

##### 球的数值（8个）

| 编号 | 标签 | 参数 | 修改修饰符 | 球的体现 |
|------|------|------|-----------|---------|
| 01 | 增伤(%) | value=50 | dmg_mult += 0.5 | 球伤害×1.5 |
| 02 | 减伤(%) | value=30 | dmg_mult -= 0.3 | 球伤害×0.7 |
| 03 | 增伤(固定) | value=10 | dmg_flat += 10 | 球伤害+10 |
| 04 | 减伤(固定) | value=5 | dmg_flat -= 5 | 球伤害-5 |
| 05 | 加速(%) | multiplier=1.8 | speed_mult *= 1.8 | 球速×1.8 |
| 06 | 减速(%) | multiplier=2.0 | speed_mult /= 2.0 | 球速÷2 |
| 07 | 加速(固定) | value=100 | speed_flat += 100 | 球速+100 |
| 08 | 减速(固定) | value=50 | speed_flat -= 50 | 球速-50 |

##### 球的飞行行为（7个）

| 编号 | 标签 | 参数 | 球的体现 |
|------|------|------|---------|
| 09 | 追踪 | turn_speed=3.0 | 球每帧转向最近敌人 |
| 10 | 避障 | — | 待场地系统 |
| 11 | 回旋 | return_distance=0.5 | 球飞到一半距离掉头返回 |
| 12 | 直行 | — | 禁用所有轨迹修改，纯直线 |
| 13 | 精准锁定 | — | 发球时方向自动对准最近敌人 |
| 14 | 扩散 | split_count | 碰撞时分裂多目标（待实现） |
| 15 | 穿透 | — | 击中人后球不停止，可连击 |

##### 球的范围（2个）

| 编号 | 标签 | 参数 | 球的体现 |
|------|------|------|---------|
| 16 | 范围扩大 | radius=80, damage_pct=0.5 | 命中时以目标为圆心80px内AOE，额外50%伤害 |
| 17 | 范围缩小 | multiplier=0.5 | AOE半径缩小 |

#### 六、Bug修复

- `load().new()` 类型推断报错（3处）
- 技能触发时序bug：标签执行后球launch会reset修饰符导致标签失效 → 改为技能触发前reset，标签执行后直接生效到投球
- `_build_tag_params()` 从空字典改为从 skill_data.tag_params 读取实际数值
- `_get_skill_data()` 从硬编码改为动态加载 skills.json

### 涉及文件汇总

| 文件 | 操作 | 说明 |
|------|------|------|
| `data/spirits/tags_registry.json` | 新建→多次更新 | 80个标签注册表 |
| `data/spirits/skills.json` | 修改 | 猛虎金刚闪+标签配置 |
| `scripts/systems/spirit_system/spirit_skill_trigger.gd` | 新建 | 技能触发器（参数读取+动态加载JSON） |
| `scripts/systems/spirit_system/spirit_tag_effect_handler.gd` | 新建→多次更新 | 效果处理器（17个对球函数实现） |
| `scripts/systems/spirit_system/spirit_system_manager.gd` | 新建 | 系统管理器 |
| `scripts/battle/ball.gd` | 修改 | 对接修饰符+追踪/回旋/穿透/精准锁定/AOE |
| `scripts/dev_tools/dev_settings_main.gd` | 新建 | 开发者工具主入口 |
| `scripts/dev_tools/dev_data_sync.gd` | 新建 | 数据同步器 |
| `scripts/dev_tools/dev_player_panel.gd` | 新建 | 球员管理面板 |
| `scripts/dev_tools/dev_spirit_panel.gd` | 新建 | 元灵管理面板 |
| `scenes/dev_tools/dev_settings_test.tscn` | 新建 | 独立测试场景 |
| `scripts/ui/main_menu.gd` | 修改 | 接入开发者工具按钮 |
| `docs/元灵技能标签确认表.md` | 新建→多次更新 | 标签确认表（重整版） |
| `docs/元灵技能标签设计表.md` | 新建 | 完整标签设计文档 |
| `modules/spirit_system/README.md` | 新建 | 系统架构文档 |

### 待手动验证

- [ ] 猛虎金刚闪：增伤50%+AOE范围伤害实际生效
- [ ] 主菜单"快捷设置（开发者）"按钮正常打开
- [ ] 球员管理：选择/修改滑块/确认保存到JSON
- [ ] 元灵管理：新建技能弹窗/标签选择/确认创建

### 待完成任务（承接）

- [ ] 对场地(FIELD)标签效果实现（12个）
- [ ] 对球员(PLAYER)标签效果实现（51个）
- [ ] 扩散标签的分裂球逻辑实现
- [ ] 能量/冷却系统与现有系统对接
- [ ] UI反馈系统对接
- [ ] 元灵属性加成到球员的对接
- [ ] 球员替补功能完善
- [ ] 发球辅助线 UI 优化
- [ ] 外围系统：招募系统、基地系统、交易系统
- [ ] 场地 3D 模型颜色修复
- [ ] 6 个球员数值平衡调优
- [ ] 音效和特效
- [ ] AI进阶：路径规划、团队协作战术

---

## 2026-06-03

### 今日完成

#### 一、体力/能量条显示位置迁移

- `player.gd` 移除头上条 → `battle_hud.gd` 底部面板+顶部对方体力条
- `battle_manager.gd` HUD绑定球员引用

#### 二、攻击力减半

6角色攻击力全部÷2（38/30/33/35/43/30）

#### 三、僵直机制

被球击中触发僵直，时间与韧性反比四段（0.7/0.5/0.4/0.3s）

#### 四、备战界面与比赛HUD分离

#### 五、元灵技能系统UI面板（6元灵15技能）