# 决竞球 Battle Ball - 工作日志

## Tips（置顶）
- 每次对话写入当日工作日志
- 记得更新前一天的任务状态

---

## 2026-06-20

### 今日完成

**球员3D素材制作流程咨询 + 教程文档**
- 摸清项目现状：球员是纯2D占位（ColorRect圆圈），`assets/characters/` 全空，零3D节点零SubViewport
- 识别隐藏风险：整个战斗系统是2D架构（CharacterBody2D），若走纯3D重构会冻结AI调优进度
- 与主人确认4个关键决策：2.5D挂载(非纯3D) / 卡通Q版猪猪侠风 / 4套动作(idle/跑/投/受击) / 先做1个试点
- 新建 `docs/球员3D建模详细教程.md`（~450行）：混元3D生图→Blender清理→Mixamo/Motion绑骨→Godot导入全流程，含踩坑表、时间预算、Mixamo vs Motion路线对比

**player.gd 2.5D开关挂载改造（方案B）**
- 架构决策：物理层2D完全不动，只把球员的"皮"换成可选挂载的3D模型
- 新建 `scenes/battle/player_model_3d.tscn`：2.5D渲染单元（SubViewport+Camera3D正交+DirectionalLight3D+ModelSlot槽位）
- 改 `scripts/battle/player.gd` 4处：
  - 顶部加 `USE_3D_MODEL` 开关常量（默认false，AI模拟走2D占位）
  - 重构 `_setup_visuals()`：拆成 `_setup_2d_avatar()` + `_setup_3d_model()`，加幂等保护修老bug（_ready和initialize各调一次导致节点重复创建）
  - 新增 `_teardown_visuals()`：配合幂等清理视觉子节点
  - 状态同步函数（enter_catch_state等4处）加 `if state_indicator:` 防御守卫
- 验证：`run_sim.sh 3 6 1 80` 跑3场，**危险总数=0**，硬规则3/3通过，比分0-3/0-1/1-0传球率100%卡死0，与基线一致（零回归确认）

### 待完成任务
- [ ] 主人按教程做第1个球员素材（混元3D生图→Blender→Mixamo→glb）
- [ ] 拿到 glb 后接入 player_model_3d.tscn 的 ModelSlot + 调动画切换逻辑（移动触发run动画等）
- [ ] ⚠ 菲菲压发球线（6.19遗留，未彻底修复，留作下轮专题）
- [ ] ⚠ 技能轮廓UI失效（6.19遗留，等主人提供诊断日志）
- [ ] P2 影响力地图接入站位（需先做位置缓存机制）

### 下一步方向
- 等主人交付第1个球员 glb 素材，做动画接入和2.5D显示调试
- 或先处理菲菲压线/技能轮廓两个遗留问题（主人定优先级）

---
