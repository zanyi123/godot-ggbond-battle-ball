# 2026-09-26 工作日志 —— 元灵技能AI多窗口并行实施（首日）

> 本日为多窗口开工方案落地首日：增益窗口（波A+工单08）、布场窗口（波D）、生存窗口（波B）三窗口并行。
> 各窗口按 05§二纪律"日志只追加"各自写段，互不删改。

---

## 生存窗口段（波B：生存/控制族27标签 + 事件钩子层）

### 完成内容

- **开工程序（07§②）**：project-context → 看板认领波B → 06契约/05§二纪律/04§1.2波B四行/02确定性纪律/副系统-事件触发响应系统方案全读 + 03/04思想浅读（"候选生成可按技能写规则，选择与调度永远收口通用评估器"）
- **registry 核对**：27标签全部存在且 id 逐字确认；实配技能仅3个（冰雪_1减速/草木_1回血/雷火_3定身）；关键实证=`move_slow` 的 multiplier 为除数语义（`1.0/multiplier`，base_route.gd:654）——越大越狠，线性计价方向正确
- **primitives_b.json（新）**：27标签描述符——状态13/体力6/运动4/控制4；计价口径 duration类=每秒价值、multiplier类线性、`value_param="none"`（on_hit_expire/unroot 无数值参数）恒回落 value_min（06§2.2缺键规定的合规用法）；gate 全用现有12键零扩展；27标签全过 06§6.1 J-JSON 自检
- **primitives_b.gd（新）**：四函数照 06§4.1 冻结签名；12 gate 与波A同构**自包含**（白名单分文件互斥设计，避免跨窗口类依赖）；波B特有增强=`reaction_hot` ctx可选字段（缺省 false fail-closed）——受击/被控反应热窗口内 self_threatened 通道打开且 bonus=1.25、calm_state 压制；ally 条目兼容 Dictionary/球员对象两种格式（字典条目下 is_instance_valid 会误过滤——波A同位置有此隐患，属他人施工中文件未动，已在本套件规避）
- **event_hooks.gd（新）**：受击瞬间反应钩子层（0.5s轮询做不到的时机）——订阅 EventBus 的 HIT_TAKEN/STATUS_APPLIED → 周期TTL热窗口（REACTION_TTL_CYCLES=2，set_cycle 注入决策周期时钟，零墙钟零randf）；头注附集成窗口三步接线说明（07§②定位：只交付钩子层+说明，接线归集成窗口）
- **test_spirit_ai_wave_b.gd（新）**：53断言——J-JSON 5（registry对账/字段/约束/value_param逐一核对）+ G-Gate 15（12键正反例/未知键fail-closed/两档bonus/reaction_hot增强）+ V-Value 8（四族计价/clamp双边界/缺参数/none）+ H-Hooks 8（订阅/热窗口/TTL过期/STATUS_APPLIED/detach幂等/clear）+ I-集成2 + R-纪律2

### 验收

- 套件 **53/53 全绿**（headless）
- sim 门禁：**危险=0**；当前树两遍 3种子**逐位一致**（未接线零扰动证明）
- ⚠ 备案：seed3 相对 baseline_run_20260925 锚定值位移（4-0/88.2%/179切→0-4/90.5%/330切）——归因树上他人正当演化（85f38e2 20工单挂接+skills.json 未提交改动），证据链=波B四文件全 untracked 零接线+两遍逐位+seed1/seed2 与基线逐位一致；与波A/布场"sim门禁=对当前树逐位复现"口径一致
- commit `4b6f185`（白名单四文件+本段日志+看板）

### 节奏决策备案

- 07§② 原文要求函数实现等波A"接口跑通"标记；波A施工中未标记。因主人指令"按照skill自行测试直至完成任务"+06契约已冻结到签名级+波B文件与波A白名单零交集（并行无冲突，返工面仅限波B自身），选择提前实现；后见布场窗口同为"主人令直接开工"先例，口径一致

### 待办 / 待批（问题登记区）

- [ ] Q1（待批）：family 枚举扩展 v1.1（player_status/player_hp/player_motion/player_control 四枚举）——family 纯元数据零逻辑风险，若否决改回纯数据操作
- [ ] Q2（待批）：gate 增强键 self_rooted / enemy_low_hp（现有12键先行，非阻塞）
- [ ] Q3（备案）：cc_immune 理想时机归主题⑤ ENEMY_ULT_WARNING，接线后 JSON 仅换 gate 键名
- [ ] 集成窗口接线时需知：ctx.allies / ctx.reaction_hot 两个可选字段的组装（event_hooks.gd 头注三步说明）

---

## 布场窗口段（波D：场地族10标签位置原语）

### 完成内容

- **开工程序（07§③）**：project-context → 看板认领波D → 05§一§二白名单纪律/06契约/04§1.2波D行/02确定性纪律全读 + 核心问题思想浅读 + spirit_ai_manager 四个场地选位雏形（`_select_field_position/_select_wall_position/_select_aoe_position/_select_area_position`）只读精化
- **registry 核对**：10标签全部存在且 id 逐字确认（区域6+障碍4）；value_param 逐一核对 registry params 数组（如 drain_wall 用 duration、shield 用 hp、obs_clear 用 clear_count）；`player_shield_obstacle` registry target_type=player 但 AI 侧按放置语义走 field_position（notes 已注明）
- **primitives_d.json（新）**：10标签描述符，schema_version=3（v1冻结字段原样+波D扩展，登记Q4）；timing_gate 全用 06§三现有12键零扩展；计价 duration类=每秒价值、danger按damage_value、shield按hp、heal按heal_per_sec
- **primitives_d.gd（新）**：契约四函数照 06§4.1 冻结签名（get_descriptors/timing_gate/compute_value/resolve_target_mode），12 gate 与波A/波B同构自包含；波D扩展 `select_field_position(descriptor, sad, ctx)`——把 manager 三雏形参数化精化为六种放置语义：goal_ball_line（0.6系数cap80）/enemy_carrier_path（0.4系数cap60）/enemy_cluster（defense=最近敌·其余=最残血敌）/shoot_line_block（0.5系数cap100，vision_block挡射手视线）/advance_lane（朝敌门60px）/defensive_anchor（球门前+20y）；**感知全走 ctx（缺键 fail-closed 回自站位）、零 randf、平局取遍历序首个（确定性）**；敌持球者判定兼容 manager 单口键（enemy_carrier/enemy_carrier_visible）与可见敌扫描双通道
- **test_spirit_ai_wave_d.gd（新）**：67断言——J-JSON 9（registry对账/字段集全等/枚举/value_param逐一核对/数值域）+ G-Gate 25（12键正反例/未知键/空描述符/缺player fail-closed/两档bonus）+ V-Value 6（两族计价/clamp双边界/缺参/none）+ P-Position 12（六语义几何精确断言/fail-closed回落/单口注入/同输入恒同）+ I-集成 6 + R-纪律 3

### 验收

- 套件 **67/67 全绿**（headless）
- sim 门禁：**危险=0**；当前树两遍 3种子玩法指标**逐位一致**（CSV仅CPU墙钟时长差0.1s，基线已备案时长=墙钟）；seed1（3-1/100%/68切）seed2（2-3/75%/158切）与 02工单锚定逐位一致
- **跨窗口互证**：seed3（0-4/90.5%/15击中/330切）与生存窗口独立跑位逐位一致——两窗口互证当前树确定性与本波零扰动；另复跑 02地基套件 17/17 绿
- seed3 相对 baseline_run_20260925 位移归因同波B备案（85f38e2 等树上演化），证据链=波D三文件全 untracked 零接线+两遍逐位+seed1/2锚定一致
- commit `d62b19a`（白名单三文件）；看板销项+本段日志随销项 commit

### 节奏决策备案

- 07§③ 原文要求函数实现等波A"接口跑通"标记；未标记即开工依据=主人令"按照skill自行测试直至完成任务"+06契约冻结到签名级+波B已开提前实现先例+波D文件与波A白名单零交集；已遵 07§③ 把 target_mode 扩展按预告申请（Q4）而非自行拍板

### 待办 / 待批（问题登记区）

- [ ] Q4（待批）：波D schema 扩展 v3（①target_mode=field_position ②family=field_area/field_obstacle ③描述符 position_intent 字段7枚举）——三者为纯分类元数据，契约三函数不消费；若部分否决回退对应项（target_mode→none+自站位兜底）
- [ ] Q5（备案）：波D ctx 扩展键清单（ball_position/own_goal/enemy_goal/enemy_carrier/enemy_carrier_visible/ally_stamina_ratios/ball_in_flight/combo_setup_active）——集成窗口接线时组装，缺键 fail-closed 已实现不阻塞

---

## 增益窗口段（波A：纯增益/资源族39标签 + 08集成基建兼做）

### 完成内容

- **开工程序（07§①）**：project-context → 看板认领波A（兼做08行，主人令"重点做工单05/06/07/08"）→ 05§二纪律/06契约/04§1.2波A行/02骰子范式全读 + 核心问题与03/04问答思想浅读；开工三查上报02工单改动在树未commit（不碰）
- **registry 核对**：39标签全部存在且 id 逐字确认（BALL数值8+PLAYER属性16+能量8+元灵7）；value_param 逐一核对 registry params 数组（发现规约建议表两处与 registry 不符：ball_speed_up_flat 实为 value 非 multiplier；元灵族 uses_up→bonus_uses、double/half→duration，建议表未指定）
- **primitives_a.json（新）**：39标签描述符，06§2.1 schema 冻结字段零新增；value_unit 按「对齐兜底分15、cap≤90」原则逐标签定价（如 multiplier≈1.3×30=39分）；**energy_cost_pct/flat 与 spirit_half 按 06§2.3⚠ 条款以 registry 描述为准裁为敌向 debuff**（与建议表相反，登记Q6）
- **primitives_a.gd（新）**：契约四函数照 06§4.1 冻结签名；12 gate 正反例全实现，bonus 两档制（self_threatened 敌距<100px 强触发 1.25，禁更细）；未知 gate 键/空 descriptor/ctx 缺键一律 fail-closed；**ally_injured 需 ctx 增补 allies**（Q7 登记，缺省 fail-closed 不阻塞）；踩坑修复：Godot `Object.get()` 只收单参（与 `Dictionary.get()` 不同），2参调用致脚本错误+套件挂起，改安全读取 `_ratio_of`
- **08集成基建（主人指派兼做）**：switches.json（默认全关）+ primitive_registry.gd（聚合 primitives_a~e.json 按顶层 wave 打标、get_descriptor 单口拦截——查得描述符先过波次开关，未开返回{}=调用方自然走 fallback，manager 零感知；同 tag 跨波冲突后加载者覆盖+告警；缺失/损坏/非法值/未知波次字母一律视同全关）——波A集成时开关天然就位，主游戏默认零污染，异常时开关即止血阀
- **test_spirit_ai_wave_a.gd（新）**：49断言——J-JSON 6（39标签集合全等/schema字段全等无多余/枚举/gate冻结键/value_param逐一在registry params/min≤cap≤90）+ G-Gate 20（12键正反例+未知键+空descriptor+两档bonus）+ V-Value 7（三族计价/clamp双边界/缺参回落）+ I-集成5（真实表39条/缺文件/坏JSON不崩溃/resolve_target_mode）+ R-纪律2（源码无 randf(/randi( 调用，注释禁令字样不算）
- **test_spirit_ai_switches.gd（新）**：19断言——S1默认（无文件/全关→任意tag返回{}+仓库默认文件master=false）+ S2总开关（开→查得/false→旁路）+ S3分波（仅A开→A查得/B存在但{}/未知波次字母视同关/开B后可查）+ S4 fail-closed（坏JSON/缺waves/master非布尔/波值非布尔）+ S5聚合（wave打标/冲突覆盖）+ R-纪律2；测试注入全走 user:// 临时文件零仓库污染，结束恢复真实开关态

### 验收

- 波A套件 **49/49 全绿** + 08开关矩阵 **19/19 全绿**（headless）
- sim 门禁：`run_sim.sh 3 6 1 80` **两遍危险=0**；游戏指标两遍逐位一致（仅 seed3 墙钟时长 17.2/17.3s 差）；seed1（3-1/100%/7击/68切）seed2（2-3/75%/12击/158切）与 02工单锚定**逐位一致**
- **跨窗口互证**：seed3（0-4/38-4接球/90.5%/15击/280伤/330切）与布场窗口两遍独立跑位逐位一致——树位移为确定性演化（同波B/D归因：85f38e2 等他人正当演化），本窗口6文件全 untracked 零接线零扰动实证
- 警告3项全为已知现象：seed2 传球率75%（基线警告带）+ 外场接球0×2（baseline known_observations）
- commit `145ee43`（白名单恰好6文件：波A三件套+08三件套）；看板销项+Q6/Q7登记+本段日志

### 工装备案

- 套件运行须**文件重定向**而非管道捕获（02工单同源坑：管道致 Godot 写阻塞挂起，本窗口实录复现一次）；headless `-s` 运行不生成 .uid，与波B/D交付形态一致

### 待办 / 待批（问题登记区）

- [ ] Q6（待批）：registry 裁决追认——energy_cost_pct/flat 敌向、spirit_half 敌向、元灵族 value_param 选 bonus_uses/duration（纯 JSON 改动，否决即回退）
- [ ] Q7（备案）：ctx 扩展键 allies（ally_injured 用）+ ball_in_flight/combo_setup_active（预留键）——集成窗口接线时组装，与Q5同类合并处理
- [ ] 集成窗口接线时需知：波A 39描述符经 registry.get_descriptor 消费（开关默认全关）；timing_gate ctx 组装按 06§4.1 + Q7 增补键

## 操球窗口段（波C：球行为族17标签 + AI虚拟操作者）

### 完成内容

- **开工程序（07§④）**：project-context → 看板认领波C（波A行✅145ee43 开工条件达成）→ 05§二纪律/06契约/04§1.2波C两行/02骰子范式/03§3.2方案a（AI输入源架构裁决）全读 + 核心问题与问答思想浅读；开工三查上报工作区他人未提交改动（ai_manager/spirit_ai_manager/spirit_tag_effect_handler/data三件/run_sim/baseline），不动不碰
- **registry 核对**：17标签全部存在且 id 逐字确认（飞行行为14+命中1+范围2）；value_param 逐一核对 params 数组（ball_avoid/straight/lockon/stealth/penetrate/sure_hit 六标签 params=[]，按波B口径 value_param='none' 恒回落 value_min 平价）
- **primitives_c.json（新）**：schema_version=4（Q8 待批）：family 扩展 ball_flight/ball_hit/ball_range；描述符新增 operation_mode（none=注入族AUTO/steer=OP_STEER/midfly=OP_MIDFLY）与 op_policy（recall/boost），均纯分类元数据契约三函数不消费；target_mode 全=ball（v1 冻结枚举零扩展）；注入族14标签 gate=ball_hold_engage（投前），操控族2标签 gate=ball_flight（06§三预留键本波启用），ball_range_down 敌向 enemy_carrying_visible；操控类按「AI 开得一般」保守定价（cap≤60，04矩阵波C行）
- **primitives_c.gd（新）**：契约三函数照冻结签名；12 gate 正反例全实现（ally_injured 兼容波B allies 对象表/波D 比值表/裸数值三形态）；未知 gate/空 descriptor/ctx 缺键一律 fail-closed
- **ai_input_source.gd（新，本波核心）**：AI虚拟操作者=意图生成器+应用器（03§3.2方案a）。generate_operation_intent：steer 瞄准优先级=敌门>最近敌拦截>保持航向>零向量（退化直线，registry 语义）；midfly 时机策略 recall（球远>350px 或 敌近球<120px 防截）/boost（球朝敌门/最近敌飞行），周期节流 exec_count 偶点放行（1.0s 间隔>300ms 双击窗，确定性无状态）；apply_operation：steer=ball.manual_steer（与 input_manager.inject_steer_to_ball 同口同闸门 _manual_active），midfly=公开入口 on_skill_key_pressed 同键再按（RELEASING 态 OP_MIDFLY 干预），**零直调 use_skill**；attach/detach fail-closed（旧状态机无口静默）
- **skill_state_manager.gd（单写者认领，最小三处）**：①ai_virtual_inputs 登记+set/get_ai_aim+clear 四口 ②_trigger_midfly 球源解析插 AI caster.ball_ref 次优先（原链只认人类 controlled_player，AI 无 input_manager；未登记字典恒空=零行为差异）③cleanup_player 清 AI 登记。未接线时与无此代码等价
- **test_spirit_ai_wave_c.gd（新）**：101断言——J-JSON 12（17标签/registry逐字/13字段全等无多余/枚举含Q8两新字段/operation_mode联动契约 midfly↔ball_flight/family 14+1+2/mode 14+1+2）+ G-Gate 24（12键正反例+ball_flight本波启用/缺省fail-closed）+ V-Value 7（三族计价/clamp双边界/缺参回落/none平价）+ O-操作意图 14（steer四级瞄准/recall三态/boost两态/周期节流/未知mode与policy fail-closed/同输入恒同）+ K-状态机 17（出厂态空/attach·detach/set·get_aim/_trigger_midfly AI球源命中+次数扣减/未登记零行为差异/apply全链 steer 走球侧同口+midfly 公开入口调用面/cleanup清理）+ I-集成6 + R-纪律 9（两新文件无随机/无墙钟/无感知直连/禁旁路无 use_skill 字样）

### 验收

- 波C套件 **101/101 全绿**（headless；调试三实录：新脚本 class_name 未进全局缓存被按名引用致解析失败→ai_input_source 本地词表副本+I6 词表恒同断言；_ratio_of 补裸数值形态；R组红线扫中注释禁令字样→改措辞）
- 存量回归 9 套：routing 21/21、e2e 27/27、op1_patch 40/40（skill_state_manager 重度覆盖零回归）、foundation 17/17、switches 19/19（registry 聚合新增波C零破坏）、waves a/b/d 49+53+67 全绿；**唯一例外 test_operator_selector 21/22**=02工单已备案存量例外（操3 e2e_* 技能踩操2过时假设，归属操2/操3轨道），与本波无关
- sim 门禁：`run_sim.sh 3 6 1 80` **两遍逐位一致、危险总数=0**；seed1（3-1/100%/68切）seed2（2-3/75%/158切）与 02 锚定逐位，seed3（0-4/90.5%/330切）与波A/波B/波D 互证的当前树值逐位——**未接线零扰动实证**；警告3项全为已知现象（外场接球0/seed2 传球率警告带）
- commit（白名单恰好5代码文件+看板+日志）；看板销项+Q8登记

### 待办 / 待批（问题登记区）

- [ ] Q8（待批）：波C schema v4——family 三枚举/operation_mode/op_policy 四项 + ball_range_down 敌向追认 + multiplier 平价（纯 JSON+元数据，否决按备案回退项操作）
- [ ] 集成窗口接线时需知：①波C描述符经 registry.get_descriptor 消费（开关默认全关）②操控族 ctx 需 ball_position/ball_velocity/enemy_goal（缺省 fail-closed）③ai_input_source.attach(state_manager, player_id, caster) 挂接、apply_operation 需接线方传 slot ④全链公开路径断言（真实 OP_MIDFLY 技能走 on_skill_key_pressed）待 skills.json 有该 operator 技能后补
- [ ] 工作区他人未提交改动（ai_manager/spirit_ai_manager/spirit_tag_effect_handler/data三件/run_sim/baseline）仍挂树上，本窗口全程未碰，提请归属窗口留意

---

## 集成窗口段（波A/B/C/D 集成接线：spirit_ai_manager 原语层单写者）

### 完成内容

- **开工程序（05模板T2）**：project-context → 看板认领集成接线行 → 05§一§二/06契约（含§八开关契约）/08工单（含§六SOP）/07指令全文读 + 核心问题与03/04思想浅读；开工三查：02前置地基改动在树未commit（四波窗口三查上报在案）→ **先代为单独落库 `5dce8bc`**（ai_manager/spirit_ai_manager/run_sim/baseline/地基套件五文件，含"主人批'按你的做'"批注内容），保证接线 diff 纯净可归因
- **registry 扩展（1函数）**：`get_descriptor_entry(tag_id)` 带波次查询（返回 {descriptor, wave}，开关语义与 get_descriptor 完全一致）——manager 侧波字母→契约函数表分发所需；get_descriptor 原口径零改动（08开关矩阵 19/19 不受影响）
- **manager 五处接线（06§五集成契约）**：
  ①`_analyze_single_skill` 增 `raw_tags`（归一化前原始 tag_id，`_normalize_tags` 重构为复用新提取函数）
  ②`_compute_base_value` 原语计价优先：任一 tag_params 键命中描述符 → 逐标签按波分发 compute_value×多标签权重(1/0.7/0.5/0.4)；未覆盖标签计旧分支未知标签缺省 10 分；全未命中（含开关全关）→ 旧三大类分支逐位不变
  ③`_decide_skill` 评分循环前逐标签 timing_gate：OR 放行、bonus 取通过者最大值乘入 score、不过则本周期跳过（无惩罚）；ctx 单键字典惰性组装（每决策周期至多一次）
  ④`_determine_intents` 意图校准（06§七）：命中标签的 descriptor.intent/intent_strength 以 max 并入后统一归一化
  ⑤`_select_field_position` 波D放置语义委托：首个命中 position_intent≠none 的描述符 → `select_field_position(descriptor, sad, ctx)`；原语 fail-closed 回自站位
- **波B事件钩子三步挂接（event_hooks.gd 头注处方）**：懒 attach（首物理帧在树内取 BattleEventBus，总线不可得静默降级）+ `_decide_skill` 顶部 set_cycle(skill_decide_count) + ctx.reaction_hot 组装
- **波C AI输入源 attach**：首决策时懒登记（battle_manager→input_manager→skill_state_manager 链，松类型 get+has_method fail-closed）；**操控族激活窗 tick 留后续工单**——skill_state_manager 缺"激活技能 id/slot 查询口"（波C单写者已收官不扩），且当前实配技能零操控族，登记即绪、tick 无法触发
- **ctx 组装（06§4.1 冻结字段 + Q5/Q7/Q8 增补键，感知全 manager 单口）**：fov_ap（02感知口）/visible_enemies（FOV 过滤）/stamina_ratio/energy_ratio/has_energy_blocked_burst（最高 base_value 主动技能被能量或冷却卡住）/threat_radius=200/allies+ally_stamina_ratios 同源双形态（波A/B 字典表、波C/D 比值表）/reaction_hot/ball_position/ball_in_flight（=is_active）/ball_velocity（=ball_direction×ball_speed）/own_goal/enemy_goal/enemy_carrier(±visible)；combo_setup_active 留⑤协议未组装
- **test_spirit_ai_integration.gd（新）**：41断言——S组9（registry entry 开/关/分波/C/D）+ V组8（全关旧公式逐位 33.75/38.28、命中/clamp上限/缺参回落/双标签权重/部分覆盖10分补齐/A+B混合波分发）+ G组7（全关不参与/root正反例/OR放行/bonus取大1.25）+ I组10（ctx 比值/球门键/球面键/队友双形态/pre_burst 三态）+ F组3（放置委托 fail-closed/(220,0)几何/波D关走旧分支）+ N组2（校准 max 并入归一化 1.3→1.4 判别）+ R组3（全关恢复）

### 验收

- 套件：集成 **41/41** + switches 19/19 + foundation 17/17 + waves a/b/c/d 49/53/101/67 + routing 21/21 + e2e 27/27 + op1_patch 40/40 全绿；test_operator_selector 21/22=02工单备案存量例外（归属操2/操3轨道，未变化）
- **sim 门禁（08§六 SOP 全流程）**：全关三种子与当前树锚定**逐位一致**（3-1/100%/7击/68切、2-3/75%/12击/158切、0-4/90.5%/15击/330切，危险=0）——**接线零扰动实证**；开A（3-0/83切、3-0/492切⚠、2-3/171切）→开AB（68/329/124切）→开ABD（68/398/201切）→全开ABCD（与ABD逐位一致=**波C零实配操控技能零贡献实证**）——五轮门禁**危险全部=0、卡死全部=0**
- diff 归因：实配技能标签 A/B/D 全覆盖（雷火_1/2、金刚_1、冰雪_1、草木_1、梦幻_1、雷火_3、大地_1、草木_2、雷火_4），开波后行为变化全部来自这些技能的描述符计价+时机闸门（如 root 改为"敌持球可见"才出手）；A单开 seed2 切换492 警告（>400 上限）在 AB/ABD/全开组合消失，非阻塞已备案
- **交付态 switches.json 保持全关**（原样字节还原零 diff）——Q1~Q8 待批期间主游戏零污染，开波=改 JSON 即生效（止血阀语义），无需动代码

### 节奏决策备案

- 接线粒度按 06§五四条+两波附言（钩子三步/输入源 attach）执行；波C 操控族 tick 与"波E+拾遗"同列后续工单；target_mode 字段接线未消费（目标选择仍走旧 `_select_player_target`，06§五未列该条，动态目标化归后续版本）
- 开波验证用临时改 switches.json（谁开谁登记），验收完字节级还原全关；02 地基落库系代为留痕（规划窗口成果），归属如需调整由主人裁

### 待办 / 待批

- [ ] 波C操控族激活窗 tick 工单（需 skill_state_manager 增激活技能 id/slot 查询口——届时由集成窗口单写者认领；首个实配 operator 技能落地前无迫切性）
- [ ] 波E拾遗 10 标签排期后同流程接线（registry 聚合已预留 primitives_e.json 位）
- [ ] Q1~Q8 规划窗口批复后，主人择机开波（推荐顺序：先A+B观察，再D，C 随首个操控技能）
