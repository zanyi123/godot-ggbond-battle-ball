# 2026-09-15 工作日志 — 夜间4项模型优化（zcode）

## 任务1：战裙穿模修复 ✅

**对象**：player2_low_walk（走路动画 v2-glb，`建模素材库/3D模型素材/player2/player2_low_walk_fixed.glb`）

**诊断（数据驱动）**：
- 裙摆区 4531 顶点中 525 个主骨绑在左右大腿骨 → 走路时裙摆被腿拽动
- 手-裙全程最小距离扫描：帧31/37/55 真实相交（min=0.001~0.003）
- 右手带 1182 顶点网格，摆臂甩到 z=0.158（裙摆区）→ 穿模真凶是手臂摆幅+裙摆随腿

**修复**：
1. 权重手术：4564 个裙摆顶点（骨距>0.12、髋高以下、带腿权重）的腿骨权重全部归入 Hips → 裙摆只随腰胯自然垂坠（修后位移 0.013~0.015，接近静止）
2. 骨骼微调：RightArm/RightForeArm/LeftArm/LeftForeArm 四骨摆动幅度 ×0.7（逐关键帧四元数轴角缩放）

**验收**：全程 73 帧手-裙最小距离 = **0.103**（修复前 0.001，标准 >0.008）✅ 通过
**导出**：player2/player2_low_walk_fixed.glb (29.7MB)，骨架级修正（权重）对该角色所有动画生效

**技术备忘**：验证骨骼动画必须用 depsgraph 求值网格（`evaluated_get(dg).to_mesh()`），`data.vertices` 永远是绑定姿态（本次踩坑浪费3次调用）；Blender 5.1 求值 mesh 无 `.free()`

## 任务2：手指骨骼权重分配 ✅（player2 完成，player1 待续）

**探测结论**：player1/player2 的 low_gu 模型骨架均为 28 骨标准人形，**没有手指骨**（手到 Hand 就结束）——"手指权重"前提是先加骨。

**实施**（player2_low_gu_v2）：
1. 自动构建五指骨链：每手 5 指 × 2 关节 = 10 根，Mixamo 命名规范（LeftHandIndex1/2 … Thumb1/2），沿 Hand 骨轴 0.11~0.234 指区几何定位
2. 分级蒙皮："越末越灵敏"——掌区留 Hand、中段 F1、指尖 F2 按沿轴位置渐变，共 1483 顶点重分配
3. 右手首次蒙皮失败（权重没写入）+ 指列错位（Pinky 吞 58%），用"拇指=宽度边缘靠身体中线侧"的解剖规则重排修正

**验收**：握拳测试弯 10 根指骨 40° → 双手指尖位移 mean≈0.31（驱动生效）、右掌心位移 0.000（隔离完美）✅
**导出**：player2/player2_low_gu_v2_fingerrig.glb (29.7MB)

**遗留**：
- 右手五指列宽向分布偏斜（Pinky 组 397 顶点偏多、Index/Thumb 偏少）——驱动功能正常但解剖归属不精确，待 Godot 实机目检后微调
- 指尖弯曲幅度 0.31 待目检确认是否过大
- player1_low_gu 同工艺复制未做（预算到 80% 线，暂停待指示）

### 任务2 补充：姿势翻车归因（主人目检发现手指完全变形）
- 归因验证：手指姿势归零后，求值网格 vs 绑定网格差 max=0.0016（数值噪声级）→ **网格和权重本身完好**
- 真因：自动创建的指骨没有控制滚转角（roll），局部轴方向随机；我用"绕局部Z转55°"摆半握姿势 = 绕随机轴拧转 → 手指/袖口被拽成麻花
- 教训：程序生成的骨骼必须显式设定 roll（弯曲轴=掌面法线），否则姿势不可控
- 现状：手指已复位伸直，模型完好；指骨+分级权重保留（动画系统可用），自然握姿需先修骨骼朝向

### 任务2 收口：右手蒙皮修复 ✅ + 工艺沉淀
- 根因确认：右手食指/中指权重覆盖不足（首跑失败残留），均匀切列导致指列错位
- 修复：指尖带(沿轴>0.17) 1D k-means k=4 聚类定位真实指列，聚类中心重排蒙皮
- 复测（绕世界掌法线轴弯40°，绕开roll缺陷）：**右手五指全部通过**——Index 0.682 / Middle 0.715 / Ring 0.802 / Pinky 0.812 / Thumb 0.237，邻指牵连 0%，掌心隔离 0%
- 导出：player2/player2_low_gu_v2_fingerrig.glb（覆盖更新）
- 工艺沉淀：tools/add_finger_rig.py（骨链构建+聚类蒙皮+三指标验收，可复制到其它模型；头部注释含全部踩坑要点）
- **待办**：① 指骨 roll 规范化（否则动画师摆姿势不可控，脚本已留 TODO）② player1_low_gu 复制 ③ Godot 实机目检弯曲观感

### 任务2 最终结论：视觉验收失败 ❌（诚实记录）
- 演示动画（手指握张+手臂摆动）播出后手臂被撕成飘带状巨幅变形，主人判定"完全没调好"——正确
- 已止损：删除演示动作、复位全部骨骼，数值复核偏差 0.00000，模型恢复完好静止
- 失败根因（三层）：
  1. **数值验收≠视觉验收**：三指标（位移/牵连/隔离）全过，但弯曲方向、平滑度、撕扯感这些视觉质量没测到——数值通过就宣称完成是过度声明
  2. **无可靠视觉反馈下做视觉工作**：MCP 截图管道吐陈旧帧（两次截图像素级相同），全程盲调
  3. **载体可疑**：low_gu_v2 是减面产物，细长手臂本身就边缘化，自动指骨+盲调权重在它上面属于垃圾进垃圾出
- 磁盘状态：player2_low_gu_v2_fingerrig.glb 静止姿态安全（数值0偏差）但**视觉质量未证实，建议勿用于生产**，是否删除待主人定
- 任务1（裙摆）保留：相交数值修复扎实，但手臂摆幅×0.7 同样未经目检，Godot 实机时顺手看一眼
- 预算耗尽 ~5000/5000。下次会话优先级：**先修视觉反馈管道**（截图陈旧帧问题），再重做手指（换有真实指缝的模型或混元带指模型），任务3/4顺延

### 夜间收尾：章鱼根因修复 + 现场落盘
- **章鱼根因**：手臂摆幅×0.7 踩了四元数双覆盖坑——w<0 的键（代表-6°）被轴角分解读成354°，缩放后变248°巨转，手臂甩天。此前手裙距离0.103"通过"实为手臂甩飞的假通过
- **修复**：从原版 player2_low_walk.fbx 取回手臂键，负w归一（最短表示）后正确×0.7，防御式重写4骨×4通道
- **数值验收**：手裙最小距离全程 0.114~0.131（自然间隙，非甩飞假象）✅
- **落盘**：player2_low_walk_fixed.glb 覆盖导出（29.8MB，导出时有"Multiple rotation mode"警告×10，待Godot目检确认）；walk_fix_session.blend 全现场保存
- **视口陈旧帧问题**：Blender 窗口后台化后系统跳过重绘（强制重绘仅0.01ms=没画），用户看到的是残影——已建议主人重进 Blender
- 待办不变：Godot 目检（手臂观感+导出警告）、指骨roll、任务3/4

### 深夜排雷：棱角球之谜破案 + 交付件净化
- **破案**：棱角球反复"复活" = MCP远程上下文中 use_selection 失效 + object.delete 静默失败，导致隐藏杂物球被反复打进导出GLB，每次导入都"复活"
- **动作数据审计**：场景累积14份重复动作、4套模型残骸；动作本体无混合旋转模式污染（28骨通道干净，导出警告为良性噪音）
- **核弹重建**：删全场→只导入交付件→删杂物→整场景（仅剩走路模型一对）导出覆盖交付件
- **回环验证**：帧1/30/60 bbox全部为正常行走角色形态（z 0~1.38），当前场景稳定为纯净两对象
- **教训**：远程上下文中所有 bpy.ops 都可能静默失败，破坏性操作后必须立即复核对象清单，不能信任返回值
- 最终交付：player2/player2_low_walk_fixed.glb（干净两对象+正确手臂缩放+裙摆垂坠+头部补偿），待主人Godot最终目检

### 最终判定：任务1 Godot验收失败 ❌（主人判负，全部实验品隔离）
- Godot实机目检：袖口/手依然穿透裙摆——Blender数值验收(0.40间隙)与Godot实机表现矛盾
- 深层原因判断：GLB导出存在"混合旋转模式"污染（12条警告），Blender求值与Godot解读的骨骼姿态不一致，Blender内验证对Godot无效
- **主人判定修复方向也需反省**：靠收敛手臂摆幅是治标，正解是裙摆重力下垂+裙形修正+（必要时）蒙皮权重精修，且必须以Godot实机为唯一验收环境
- **止损隔离**：三个实验品移入 归档/废案_20260915/（walk_fixed/fingerrig/失败现场blend），孤儿import清理，player2目录已净化
- **源文件零损伤确认**：player2_low_walk.fbx / low_gu 系列 / base.glb 全部完好
- 游戏本体不受任何影响（battle3d用共享动画+base模型，未引用实验品）

### 下次重做路线（先决条件成熟后）
1. 【先决】修复视觉反馈：Blender截图管道 or 直接以Godot为验收环境（改一步→Godot看一步）
2. 重做顺序：裙摆权重归髋 → 裙形重力外扩 → 手臂保持原味 → **只信Godot实机**
3. 手指任务同样等视觉管道修好再重做
4. 工艺积累保留：tools/add_finger_rig.py、tools/fix_head_tilt.py、四元数双覆盖坑、混合旋转模式导出污染等教训全部在册

### 任务4：网格拓扑修复 ✅（试点player2，待Godot目检）
- 无头Blender批处理（tools/repair_mesh_topology.py），保守三件套：合并重合顶点0.001/重算法线/删游离元素
- **player2_base.glb（游戏在用模型）**：顶点889674→532380（合并35万重合顶点），非流形边 272316→4808（**98%清除**）——黑斑/碎面病灶实锤并修复
- **player2_low_gu_v2.fbx（工作模型）**：顶点76252→58691，但非流形边 61628→61526 基本没降——低模减面造成的深度拓扑损伤，基础三件套不够，需进阶修复（补洞/成流形），先看基础版Godot效果再决定
- 产物：player2/修复/ 目录下两个修复版GLB（原件未动）
- 待办：主人Godot目检修复版 → 通过则批量修8人 + 换入assets/game_models
- 任务3（球挂手）确认顺延：依赖任务1重做后的健康模型，现在做必返工

---

# 深夜续会话 — 步骤②杯型手（player2 小样，zcode）

## 今日目标
- 跑通 player2 小样流程：①已验收 → 完成②杯型手，落实工作日志教训（只信Godot实机/不盲调/MCP操作必复核）

## 完成内容

### 1. 视觉反馈管道打通 ✅（上次会话头号先决欠账，正式闭环）
- **方案**：GL 离屏渲染（`render.opengl` + `save_render`）强制出图 → Read png 目检，不依赖窗口前台
- 陈旧帧根因三层确认：view_* operator 是 modal 平滑动画（立即读 distance=旧值）+ 窗口后台化跳过重绘（redraw_timer 0.01ms）+ rv3d 属性赋值数据生效但屏幕不刷新；另发现 `view_selected` 在 temp_override 下静默失败
- 本次 4 轮塑形全程真目检零盲调——"盲调必败"教训正式闭环，方法已沉淀进 SOP 教训7

### 2. 关键事实修正：base.glb 无骨架
- 实测 player2_base.glb 及修复版**无 ARMATURE 无顶点组**（纯静态网格）——执行计划②原写"RightHand 权重定位（1182顶点）"是 low_gu_v2 的方法，对 base 不适用
- 改几何定位：T-pose 掌心朝下（局部-Y）、角色右手=X-侧、腕线 x≈-0.38 / 指尖 x≈-0.542 / 掌心中心局部 (-0.44, 0.547, 0.022)

### 3. 步骤②杯型手 Blender侧完成 ✅
- 4 轮迭代（15%/轮铁律，每轮 GL 渲染三视角目检）：绕腕枢轴 X 轴卷角累计 45° + 指尖向掌心线内收 15%
- 全程无撕裂无破面；左手保持 T-pose 原状对照
- 球体对比（r=0.10 按球/身高比例）：球坐指碗、贴掌**不悬空不穿透** ✅
- 产出：`player2/修复/player2_base_手型版.glb`（回环复核：单对象、bbox 一致、原件零接触、无杂物复活）
- 文档同步：执行计划（环境清单+②实测工艺参数+进度看板）、SOP（教训7+已知边界）

## Lint 检查
- 纯 Blender 建模会话，无代码改动；GLB 回环导入验证通过

## 待办 / 遗留
- [ ] **主人 Godot 目检 `player2_base_手型版.glb`**（黑斑/手型/放球观感）——通过则②正式闭环
- [ ] 步骤③路线拍板（甲=动画FBX载体+贴图迁移 / 乙=网格换皮）——注意：base 无绑定，路线乙对 base 无意义，建议甲；SOP 要求模型层验收后才开工
- [ ] 批量 player1~8：①脚本直跑；②工艺参数已沉淀执行计划②节，逐人 GL 目检
- [ ] 塑形参数为 player2 实测值，其他角色手部尺寸不同需按同法重测（流程可复制，数值不可照抄）

## 踩坑记录（新）
- Blender 5.1：`view3d.view_all` 无 `use_bounds` 参数；rv3d 无 `clip_start`（挂在 space 上）
- GL 渲染走**世界坐标**：glTF 导入 mesh 局部系 Y-up、matrix_world 带 ±90°X 旋转，机位/放物必须 `matrix_world @` 变换
- `primitive_uv_sphere_add(location=)` 吃**世界坐标**——喂局部坐标会把物体埋到地下（本次"球消失"真因）
- 探测顶点注意坐标系：`v.co` 是局部（Y-up），`bound_box @ matrix_world` 是世界（Z-up），混用会拿错轴

## 预算
- 总预算 5000：本次实际消耗约 **2800**（MCP 调用约 30 次，含 4 次报错重试），剩余约 2200，未触发 80% 硬停

---

# 2026-09-15 补充（zcode · 对战玩法窗口）：空中发球双视角全套（P0/P1/P2）

## 今日目标

- 空中斜线发球核心 + 第三人称投影光标 + 第一人称准星模式，双视角玩法成套落地

## 完成内容

### 1. P0 空中斜线发球（球核心）

**文件**：`scripts/battle/ball.gd`、`player.gd`、`input_manager.gd`、`battle_manager.gd`

- `launch()` 新增 `start_z/pitch_deg`：斜线模式 z(L)=出手高+L·tanφ（直线只是斜的），触地贴地滑行
- 仅空中出手启用斜线（地面发球恒高不变，sim 基线零影响）；出手球点=跳跃高度+55
- a 方案：空中耗尽停球→球权即时分配 + 0.35s 视觉下坠（消除瞬移观感）

### 2. P1 第三人称投影光标 + 命中预览

**文件**：`battle_arena_3d_bridge.gd`、`battle_manager.gd`、`player.gd`、`ball.gd`

- `preview_path_hits()` 纯函数：直线与球员命中窗口求交（双视角共用核心，提示非锁定）
- 3D 场地地面**常驻投影光标环**：鼠标在场内即显示、出界隐藏、瞄准标中球员变红（永久性功能）
- 瞄准路径命中的球员头像黄底红边高亮

### 3. P2 第一人称准星模式

**文件**：`camera_3d_controller.gd`、`input_manager.gd`、`battle_arena_3d_bridge.gd`

- 相机 FIRST_PERSON 模式：挂眼点（跳跃高度+60）+ 沿视线前移 35px（防嵌自机模型）+ FP 隐藏自机代理
- E 键进出（独立按键）；进入捕获鼠标（OS 光标隐藏）、退出恢复；FP 下场地投影光标隐藏
- 鼠标转视角（右移右转/上移抬头，pitch 夹 [-60°,+30°]）；屏心准星，视线命中球员变红
- FP 发球=视线直发（可打任意高度含空中平射）；FP 移动视角相对（W前S后A左D右随视线旋转）
- FP 下球员朝向同步视线

### 4. 回归测试两件套（全 PASS）

- `test_air_throw.gd`（11 断言：斜线/触地/恒高兼容/命中预览/下坠视觉）
- `test_fp_mode.gd`（12 断言：进出/视角/发球/移动映射/相机前移）
- 全套回归 M2/M3/M5/AT/FP/P3 全 PASS；sim 无新增危险（seed2 偶发卡死 A/B 实证与本轮无关）

## 待办 / 遗留

- [ ] 球"击中前瞬移"完整修复（躲开者球权吸附方案，因 ball.gd 并行编辑暂缓）
- [ ] lob 技能标签接线 + AI 拦高球 + AI 空中发球
- [ ] M7 物理常量迁 JSON；FP 下 2D 瞄准线隐藏（观感优化）

## 踩坑记录

- **FP 相机嵌自机模型**：相机沿视线前移 35px + FP 隐藏自机代理（标准 FPS 做法）
- **FP 移动"前后左右对调"**：第三人称世界轴移动在视角旋转后与视线错位——改视角相对移动
- **FP 鼠标转向反向**：视线 (cosθ,·,sinθ) 右转=θ 增，初版减号写反
- **headless 下 MOUSE_MODE_CAPTURED 不可设**：测试条件跳过（真机行为）
- **裸 [] 传类型化数组参数**：运行时类型不匹配→脚本中断→quit 不执行→测试挂起（跑测试务必 timeout）
