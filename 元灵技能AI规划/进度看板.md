# 进度看板 —— 元灵技能AI多窗口并行（05方案§一配套）

> 规则：开工先认领（窗口名/时间/范围）；只更新自己那行；行带 commit hash；
> 跑 sim/全量回归前把自己的行标"验证中"；冲突以 git log 为准。
> 窗口按工作分工命名（主人裁），波次字母为任务编号；定义见 `04_主题问答_四五六_实施前.md` §1.2/§1.3，白名单见 `05_多窗口开工方案.md` §一。

| 任务 | 窗口名 | 状态 | 认领窗口 | 开始时间 | commit | 问题/备注 |
|---|---|---|---|---|---|---|
| 06_原语接口规约定稿 | 规划窗口 | ✅ 已完成 | 规划窗口 | 2026-09-25 | （纯文档） | 波A施工图纸就绪 |
| 08_集成基建：原语开关（switches.json+registry过滤+测试） | **集成窗口** | ✅ 已完成 | 增益窗口会话兼做（主人2026-09-26指派"重点做工单08"） | 2026-09-26 | 145ee43 | 套件19/19绿（S1-S5+R）；switches默认全关=主游戏零污染，开波=止血阀（08§六SOP）；与波A同commit交付 |
| 波A 纯增益/资源族（39标签） | **增益窗口** | ✅ 已完成（**接口跑通**，波B/D后继依赖达成） | 增益窗口 | 2026-09-26 | 145ee43 | 套件49/49绿（J/G/V/I/R）+sim门禁两遍危险=0逐位一致；seed1/2与02锚定逐位、seed3树位移与波D跑位互证（0-4/90.5%/330）零扰动实证；⚠登记Q6/Q7待批 |
| 波B 生存/控制族（27标签）+事件钩子 | **生存窗口** | ✅ 已完成 | 生存窗口 | 2026-09-26 | 4b6f185 | 套件53/53绿+sim危险=0（当前树两遍3种子逐位一致，未接线零扰动）；Q1/Q2待批，Q3备案 |
| 波C 球行为（17标签）+AI虚拟操作者 | **操球窗口** | ✅ 已完成 | 操球窗口 | 2026-09-26 | 768c73d | 套件101/101绿（J/G/V/O/K/I/R）+存量9套回归（唯一例外=02工单备案存量项）+sim两遍逐位危险=0（seed1/2与02锚定、seed3与波A/B/D互证值逐位，未接线零扰动）；Q8待批；**skill_state_manager 单写者三处最小改动（AI输入源口），未接线零行为差异** |
| 波D 场地族（10标签）位置原语 | **布场窗口** | ✅ 已完成（sim门禁已过，验证资源已释放） | 布场窗口 | 2026-09-26 | d62b19a | 主人令直接开工（同波B提前实现先例）；套件67/67绿+sim危险=0两遍逐位（seed3与波B跑位互证一致）；扩展申请Q4/Q5待批 |
| 波E 长尾（10标签） | **拾遗窗口** | 未排期 | — | — | — | 逐个工单 |
| 集成接线（每波完工时） | **集成窗口** | 待办 | 规划窗口/集成位 | — | — | spirit_ai_manager 单写者 |

## 已完成里程碑

| 日期 | 里程碑 | 依据 |
|---|---|---|
| 2026-09-25 | 前提调研 + 现状账 | 01 文档 |
| 2026-09-25 | 前置地基工单（确定性骰子+视野闸门+失误短冷却+基线重锚，sim 逐位可复现） | 02 工单 |
| 2026-09-25 | 主题①②③④⑤⑥问答 + 六项主人裁决全认可 | 03/04 文档 |
| 2026-09-25 | 多窗口开工方案（窗口按工作分工命名）+ 本看板 | 05 文档 |
| 2026-09-25 | 06 接口规约定稿 + 07 各窗口开工指令（复制即用，五窗口×整块）；波B标签数修正 29→27（39+27+17+10+10=103 对账） | 06/07 文档 |
| 2026-09-26 | 波A增益族+08开关基建（145ee43）、波B生存族（4b6f185）、波D场地族（d62b19a）、**波C球行为族+AI虚拟操作者（768c73d）**——五波实施窗口四波交付完毕，待集成接线与波E排期 | 本看板各行 |

## 问题登记区（各窗口设计申请，规划窗口批复）

| # | 申请窗口 | 事项 | 影响 | 状态 |
|---|---|---|---|---|
| Q1 | 生存窗口 | **family 枚举扩展**（schema v1.0→v1.1 提案）：波B四族需新增 `player_status`(13)/`player_hp`(6)/`player_motion`(4)/`player_control`(4) 四枚举；v1 三枚举系波A家族，波B硬套语义失真。family 为纯分类元数据，06 契约三函数（timing_gate/compute_value/resolve_target_mode）均不消费，扩展零逻辑风险 | primitives_b.json 已按 v1.1 起草（schema_version=2 占位）；若否决改回纯数据操作 | 待批 |
| Q2 | 生存窗口 | **gate 增强需求**（非阻塞，现有 12 键先行）：①`self_rooted`（player_unroot 理想时机=已处于定身，现顶 self_threatened）②`enemy_low_hp`（hp_damage/dot 残血收割，现顶 enemy_visible） | 归 06§三 gate 表扩展，规划窗口批后增补 | 待批 |
| Q3 | 生存窗口 | cc_immune 理想时机=ENEMY_ULT_WARNING 预警消息（主题⑤协议，未实现），现轮询版顶 self_threatened；预警接线后无需改 JSON（gate 键换名即可） | 归主题⑤轨道 | 备案 |
| Q4 | 布场窗口 | **波D schema 扩展**（07§③预告，v1基座→v3提案）：①target_mode 新增 `field_position`（10标签 target_type=field，位置选择语义）②family 新增 `field_area`/`field_obstacle` 两枚举（同Q1逻辑：family 纯分类元数据，契约三函数不消费）③描述符新增 `position_intent` 字段（7枚举：none/goal_ball_line/enemy_carrier_path/enemy_cluster/shoot_line_block/advance_lane/defensive_anchor，供 select_field_position 分发，契约三函数不消费） | primitives_d.json 按 v3 起草；若部分否决回退对应项（target_mode→none+自站位兜底） | 待批 |
| Q5 | 布场窗口 | **波D ctx 扩展需求**（集成窗口接线用，非阻塞）：位置选择需 ball_position/own_goal/enemy_goal/field_bounds/visible_enemies 朝向等键，06§4.1 ctx 未含；primitives_d.gd 对缺键 fail-closed（返回自站位兜底），不阻塞本波交付 | 归集成窗口接线工单 | 备案 |
| Q6 | 增益窗口 | **registry 裁决追认**（06§2.3⚠条款执行）：①`player_energy_cost_pct/flat` registry 描述="消耗能量"实为削能 debuff，direction=self(建议表)→**enemy**（gate 随之 enemy_visible/control）；②`player_spirit_half` "下次技能效果减半"按 reverse 配对（spirit_double 自向翻倍）裁为**敌向** debuff；③元灵族 value_param 取 registry params 实有项：uses_up→`bonus_uses`、double/half→`duration`（建议表未指定，params 缺键会恒落 value_min 失去计价梯度）。以上均 JSON 内 notes 已标注，改动零函数逻辑 | 若否决：改回 direction=self + 建议表 gate 即可（纯 JSON 改动） | 待批 |
| Q7 | 增益窗口 | **ctx 扩展需求**（集成窗口接线用，非阻塞）：①`ally_injured` gate 需 ctx 增补 `"allies": Array`（06§4.1 冻结 ctx 未含，原语禁自读感知，只能 manager 组装）；②`ball_flight`/`ally_cast_setup` 已按可选 ctx 键实现（`ball_in_flight`/`combo_setup_active`），本波不用，操球窗口/⑤协议启用时由 manager 填充。三键缺省一律 fail-closed=视同不触发，不阻塞交付 | 归集成窗口接线工单（与 Q5 同类合并处理） | 备案 |
| Q8 | 操球窗口 | **波C schema 扩展**（v1基座→v4提案，承Q1(v2)/Q4(v3)序列）：①family 新增 `ball_flight`(14)/`ball_hit`(1)/`ball_range`(2) 三枚举（同Q1逻辑：family 纯分类元数据，契约三函数不消费）②描述符新增 `operation_mode` 字段（3枚举：none=注入族AUTO/steer=OP_STEER激活窗/midfly=OP_MIDFLY飞行中干预，供 ai_input_source 分发，契约三函数不消费）③midfly 标签追加 `op_policy` 字段（recall/boost 时机策略分发）④`ball_range_down` 按 reverse 配对裁敌向；registry multiplier 越小削益越强与线性计价反向，v1 取平价（梯度化待批）⑤ctx 扩展需求（集成接线用，非阻塞）：操控族需 ball_position/ball_velocity/enemy_goal（ball_in_flight 已见Q7），缺省 fail-closed 不阻塞 | primitives_c.json 按 v4 起草；若部分否决回退对应项（operation_mode→全none，ai_input_source 改 tag 硬表分发兜底） | 待批 |
