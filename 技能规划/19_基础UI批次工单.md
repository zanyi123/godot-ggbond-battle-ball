# 19 · 基础 UI 批次工单（13 规划转工单 · 原语状态可视）

> 依据：技能规划/13 基础 UI 批次规划（2026-09-24 显示层修正版）。定位：原语可验收性的基础反馈——**看得出生效没生效**。
> 显示层裁定（09-24 主人）：**屏幕空间 HUD（状态图标条/层数/充能点）=2D；世界空间交互反馈（盾本体/预览等）=跟随显示模式，3D 主/2D 兜底**。
> 红线：08 §五 全程；本批显示件=功能性简约（色块/弧面/数字），粒子化/动作归美术 M2。
> 优先级：建议排在操3 联调前或并行（联调时禁疗/印记类"看得见"才能验全）。
> 制定：2026-09-24。

---

## 一、四件交付（13-A~D）

### 13-A 通用状态图标条（屏幕空间 2D，一次做全员复用）
- 位置：battle_hud 球员头像旁横排；图标 24px 色块（分类底色+单字符）+ 右上秒数角标 + 右下层数角标；上限 8 个折叠 "+N"
- 数据源（**只读消费既有信号，UI 零判定逻辑**）：状态灯查询（控制类既有）+ mark_changed（印记）+ shield_state_changed（盾）+ heal_block/energy_block/reflect/immune/share/toggle 等波3 新状态标志
- 临时配色表（注释挂待 ElementColorBank）：禁疗紫/禁能蓝灰/反伤橙红/免疫白框/弱点暗红/分摊青/印记绿/盾金/治疗翠绿
- 刷新：事件驱动重绘（不轮询）

### 13-B 波3 六原语接入图标条
禁疗/禁能/反伤/元素免疫/元素弱点/能量分摊/储存充能/TOGGLE 激活态——各挂图标（色块+秒数），随状态出现/消失

### 13-C 盾本体可视（世界空间，3D 主/2D 兜底）
- 3D：盾障碍实体可见形态（复用岩石墙网格族+盾色材质区分；follow=随球员、static=落点）
- 耐久显示：hp 模式=材质裂纹/变色梯度；uses 模式=剩余次数点（HUD 侧）
- 2D 兜底：_draw 弧形贴身显示（sim/2.5D 模式）
- 断言：生成可见、耐久变化可辨、碎/超时消失

### 13-D 四个小件
1. **印记层数**：目标头上/HUD 印记图标 + 层数数字（mark_changed 消费）；阈值触发瞬间闪烁
2. **充能点**：技能图标旁 N/M 小圆点（charge_stock）
3. **必中锁定线**：释放者→目标连线（复用瞄准显示通路）
4. **球隐身半透明**：2D 球代理 alpha（is_stealthed 消费）+3D 侧透明度（同参数）

### 13-E 操作反馈显示核验（自操3 移入，2026-09-24 层界修正）
> 操作系统已交付的反馈节点（operator_feedback_3d：AIM 预览路径/POINT 候选高亮/MARK 标记圈）——本批负责其**显示层核验**：
- AIM 预览路径节点：激活后生成/跟随鼠移/确认后消失（世界空间，3D 主/2D 兜底）
- POINT/MARK 候选高亮：悬停高亮生效
- 断言：节点生成/跟随/消失三态 + 跟随显示模式双轨（3D 主/2D 兜底）

## 二、文件清单（预估）

`battle_hud`（或新 `status_icon_bar.gd`）、`skill_visual_manager.gd`（2D 兜底扩展）、`skill_fx_3d_adapter.gd` 或 3D 反馈节点（盾 3D 形态）、球代理（隐身 alpha）、新测试 `test_basic_ui.gd`（节点/状态断言）

## 三、断言清单（≥13 项）

1. 图标条：状态挂载→图标出现（色/符号正确）；到期→消失
2. 秒数角标递减；层数角标随 mark_changed 更新
3. 8 个折叠 "+N"
4. 盾：生成可见（follow 跟随定位）、耐久递减可辨、uses 扣次、碎/超时消失
5. 充能点 N/M 随充能/消耗更新
6. 必中线：释放者→目标连线节点生成
7. 球隐身：is_stealthed=true 时 2D alpha 半透明、恢复不透明
8. UI 零判定断言：图标条脚本无任何游戏状态写操作（grep+架构核验）
9. 全量回归不回退
10. run_sim 1 场危险=0
11. AIM 预览路径：生成/跟随/消失三态
12. POINT/MARK 候选高亮生效
13. 双轨核验：3D 主显示 + 2D 兜底均可用（跟随显示模式）

## 四、验收

- [ ] 断言 ≥10/10
- [ ] 主人目验（观感项，标注"未目验"不影响工单关闭——观感升级归 M2）
- [ ] 回写 13 执行记录/工作日志

## 五、边界

- 不做：粒子特效/举盾动作/配色升级（M2/M4）
- 不做：ElementColorBank（临时配色表先行，注释挂统一）
- headless 截图受限：以节点/状态断言为主，截图有窗口环境补

## 六、引导词（填好版，直接粘贴）

```
按《技能规划/19_基础UI批次工单》实施基础 UI 批次四件（13-A~D）：
状态图标条 / 波3 六原语接入 / 盾本体可视 / 印记层数+充能点+必中线+球隐身。

显示层裁定（遵守）：屏幕空间 HUD=2D；世界空间反馈=跟随显示模式（3D 主
unshaded 简约件 + 2D _draw 兜底），经 battle3d bridge 单向同步。

边界红线（08 §五 + 本批适配）：
1. UI 层零判定逻辑——只消费既有信号与查询接口
   （状态灯/mark_changed/shield_state_changed/is_stealthed），禁止写游戏状态
2. 禁止粒子化/动作/配色升级（美术 M2/M4）；本批=功能性简约可视
3. 禁止改 skills.json 技能条目（R1）；禁止做其他批次
4. 禁止顺手修工单外问题；拿不准（配色/位置）→ 用临时默认值+注释标注，不阻断
5. headless 截图受限如实标注，不伪造截图

验收：工单 §三 断言 ≥10/10 + 全量回归 + run_sim 1 场危险=0；
回写 13 执行记录/本工单执行记录/工作日志，汇报只报事实数据。
```

## 七、执行记录

| 件 | 实施 | 验收 | 日期 |
|---|---|---|---|
| 13-A~D | ✅ 2026-09-25（实施明细见下） | ✅ test_basic_ui **19/19** + 全量回归 19 套件全绿（347 断言）+ run_sim 3 场危险=0 | 2026-09-25 |
| 主人目验 | ⬜ | 观感项（虚线样式/配色/位置——标注"未目验"不影响工单关闭，升级归 M2） | | |

### 实施明细（2026-09-25）

- **13-A**：新组件 `status_icon_bar.gd`（_draw 自绘 24px 色块+秒数/层数角标+充能点 N/M；8 上限折叠"+N"；事件驱动=player 整秒节流广播）；**player 侧数据源**：新信号 `status_lights_changed`（turn_on/off/tick 过期+整秒节流 emit）+ `toggle_changed(skill_id, open)` + `get_status_lights_view()` 只读快照
- **13-B**：波3 六原语灯键（heal_block/energy_block/reflect/element_immune/element_weak/energy_share/charge_stock）+ toggle 激活态入 STATUS_ICONS 映射表，灯亮即挂/灭即摘；HUD 三球员面板右上角各挂一条（setup_players 绑定）
- **13-C**：obstacle_manager 新信号 `player_shield_spawned`；2D 兜底=`shield_visual_2d.gd`（盾子节点弧面：follow/static 天然同步+hp 梯度弧长收缩+uses 次数点，skill_visual_manager 订阅挂载）；3D 主=`shield_visual_3d.gd`（简约盒体 unshaded 盾色+hp 变色，bridge 订阅建代理+每帧贴 2D 盾位姿 (x,0,y)）
- **13-D**：印记层数（mark_changed→层数角标）✓；充能点（charges/charges_max——player_route 补 charges_max 存入）✓；必中锁定线（ball 子 Line2D：sure_hit+lockon_target 两点连线）✓；球隐身半透明（ball_visual/shadow alpha=0.35）✓
- **边界遵守**：UI 层零判定（grep 断言进测试）；粒子/动作/配色升级未做（M2/M4）；skills.json 零改动；headless 截图无法产出如实标注（有窗口环境补 docs/img/）
- **实施踩坑**：`Dictionary.erase()` 返回 bool 非 int（bool>int 编译错）；-s 模式 class_name 全局缓存不含新类（HUD 改 load() 加载）；headless 物理帧不跑（秒数递减测试改手动 tick 驱动广播链路）

### 二轮复查漏缺补齐（2026-09-25，主人令"再看有没有漏缺"）

逐条对照工单原文复查出 **6 处漏缺，全部补齐**（test_basic_ui 19/19 → **29/29**）：

| # | 漏缺（工单原文依据） | 补齐 |
|---|---|---|
| 1 | 13-A 数据源列"shield_state_changed（盾）"+配色"盾金"——图标条无盾条目 | bar 加 `connect_shield_source(om)`：spawned→按 caster_id 匹配挂"盾"图标；removed（新增 manager 信号，碎/超时/手动统一出口）→摘 |
| 2 | 13-C"uses 模式=剩余次数点（**HUD 侧**）"——次数点只在 2D 弧面 | 盾图标 uses 模式复用充能点绘制（次数点随 on_ball_hit 联动 3→2） |
| 3 | 13-C 断言"碎/超时消失"未测 | 补③c（uses 耗尽自动销毁→图标摘）+③d（duration 到期自动清理→图标摘） |
| 4 | 13-B"元素弱点"列明未测 | 补①（element_weak 灯亮挂"弱"图标/灭摘） |
| 5 | 13-D2"随充能/消耗更新"只测了初始显示 | 补②（charges 4→2 角标联动） |
| 6 | 13-D4"+3D 侧透明度（同参数）"未做 | ball_proxy_3d_v2.set_stealth（兼容兜底球 material_override 与 GLB surface 材质，duplicate 防共享污染）+bridge._sync_ball 每帧同步 is_stealthed |
| 附 | 13-D1"阈值触发瞬间闪烁"未做 | 轻量近似：叠层动作触发 2 次闪烁（_process 仅闪烁期活动）；**精确"阈值触发"判定层无信号（阈值消费在 handler）——上报待判定层补信号** |
| 附 | 截图存档（§边界"headless 截图受限…有窗口环境补"） | 本机窗口环境可用：非 headless 渲染出 `docs/img/ui_status_bar_shot.png`（图标条全景） |
- **事实标注**：13-C"复用岩石墙网格族"——3D 层障碍渲染本就不存在（岩石墙无 3D 代理），盾 3D 件以简约盒体起步，岩石墙网格族复用随 3D 障碍渲染另批
