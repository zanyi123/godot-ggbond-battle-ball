# 标签整合 - 手动验证清单

> **用途**：任务4联调验证。整合前4个任务（并行验证/技能参数/管理器补建/鼠标冲突）已全部完成，此清单用于在编辑器中确认整合是否真正成功。
>
> **前置**：用 Godot 4.6.2 打开项目，所有改动已保存。

---

## ⚠️ 必读：操作要点

**坑1：比赛场景必须先点「开始比赛」按钮**
- 运行 `battle_arena.tscn` 后先显示「备战界面」
- 必须点击下方的 **「开始比赛!」** 按钮，才会进入可操作状态（`match_started=true`）
- 没点开始前，**所有按键/鼠标都被拦截，完全无反应**

**坑2：技能释放必须双击（间隔<300毫秒）**
- 按1次技能键：仅激活，不释放，没效果
- **快速连按2次**：自动释放，才有效果
- 慢按第2次：取消激活（不是释放）

**坑3：切换球员用 1/2/3 键，不是 Tab**
- KEY_1=位置0(猪猪侠) / KEY_2=位置1(超人强) / KEY_3=位置2(菲菲)
- 技能键是 KEY_4/5/6（对应技能槽 0/1/2）

**坑4：能量已临时跳过（测试阶段）**
- 代码里能量检查已临时关闭（能量回复机制待后续设计）
- 控制台会看到 `[Player][测试] 能量不足但跳过`，属于正常，技能照常释放

**坑5：验证6/7在测试面板场景，不是比赛场景**
- 验证6/7 要运行 `scenes/test/field_tag_test_panel.tscn`
- 结果在**屏幕日志面板**显示，不在控制台

---

## 验证1：比赛正常启动（冒烟测试）

**操作**：
1. 打开场景 `scenes/battle/battle_arena.tscn`
2. F5（或 F6）运行
3. ⚠️ **比赛启动后先显示「备战界面」，必须点击下方的「开始比赛!」按钮**，才会进入可操作状态
4. 点击开始后，控制台应出现 `match_started` 相关日志

**预期**：
- 控制台输出 5 个管理器创建日志：
  ```
  [BattleManager] 场地物理管理器已创建
  [BattleManager] 障碍物管理器已创建
  [BattleManager] 场地区域管理器已创建    ← 新增
  [BattleManager] 幻象管理器已创建         ← 新增
  [BattleManager] 元灵技能系统已创建
  ```
- 6 个球员全部装备元灵（日志：`[Player] xxx 装备元灵: xxx 技能=[...]`）
- 无 `push_back` 报错
- 比赛画面正常显示，球员可移动

**判定**：5 条管理器日志 + 6 条装备日志 + 无报错 = ✅ 通过

---

## 验证2：球增伤技能（BALL类·主控猪猪侠）

**操作**：
1. 等比赛开始（已点「开始比赛」），主控是**猪猪侠**（队A位置0，金刚元灵）
2. ⚠️ **技能释放不需要持球**，任意时刻都可放技能
3. ⚠️ **技能释放需要双击：快速连按两次 4 键（间隔<300毫秒）**
   - 按1次：仅激活（日志显示 `[SkillState] 激活技能`）
   - 快速按2次：自动释放（日志显示 `[SkillState] 双击检测...自动释放`）
   - 如果慢按第2次：会取消激活（不是释放）
4. 双击释放后发球：左键点击瞄准 + 松开发球，球会享受增伤效果

**预期**：
- 控制台输出：
  ```
  [InputManager] 技能已激活: skill_jingang_1
  [InputManager] 技能已释放: skill_jingang_1
  [SpiritSystemManager] 使用技能: ..., skill_id=skill_jingang_1
  [SpiritSkillTrigger] 执行技能标签: ["ball_dmg_up_pct", "ball_range_up"]
  [TagEffectHandler] 增伤: ...  或类似标签处理日志
  ```
- 发球后球的速度/伤害应体现增伤（数值可观察）

**判定**：看到 `双击检测...自动释放` + `执行技能标签` + 标签处理日志 = ✅ 通过

---

## 验证3：障碍放置技能（FIELD类·需切换球员）

**操作**：
1. 按 **2** 键（不是 Tab）切换到**超人强**（队A位置1，岩灵/大地元灵）
   - KEY_1=主控位置0(猪猪侠) / KEY_2=位置1(超人强) / KEY_3=位置2(菲菲)
2. ⚠️ **技能释放不需要持球**，任意时刻都可放技能
3. ⚠️ **双击 6 键**（快速连按两次，间隔<300毫秒）释放 skill_大地_1（岩石墙）
   - KEY_4=技能槽0 / KEY_5=技能槽1 / KEY_6=技能槽2
   - 超人强技能=[dadi_1, dadi_2, 大地_1]，只有 slot2(6键) 的 skill_大地_1 有数据
4. **此时进入鼠标放置模式** → 移动鼠标看预览
5. **左键点击**场地放置障碍
6. 放置完成后，左键瞄准+松开发球

**预期**：
- 释放技能后进入鼠标放置模式（有预览/光标变化）
- **左键点击时不会发球**（守卫生效）← 关键验证点
- 障碍物出现在点击位置（土黄色长条墙，140×28）
- 放置完成后（operation_finished），**左键能正常瞄准发球** ← 关键验证点
- 控制台输出：
  ```
  [TagEffectHandler] 创造障碍: shape=rect hp=300 atk_consume=10/s spd_consume=40px/s mouse_ops=1
  [ObstacleManager] 创建障碍物: shape=rect hp=300 pos=(...)
  ```

**判定**：
- 放置期间左键不发球 ✅
- 障碍正确生成（hp=300, 土黄色）✅
- 放置后能正常发球 ✅
- 三项全满足 = ✅ 通过

---

## 验证4：取消放置（右键/ESC）

**操作**：
1. 重复验证3的步骤1-4，进入放置模式
2. 按 **右键** 或 **ESC** 取消

**预期**：
- 放置模式退出，不生成障碍
- 右键取消不会被 input_manager 拦截成"取消瞄准"（守卫生效）
- 取消后能正常操作

**判定**：放置可正常取消，无副作用 = ✅ 通过

---

## 验证5：障碍耐久（球撞障碍）

**操作**：
1. 先放置一个障碍（验证3）
2. 切回主控，持球
3. 朝障碍发球
4. 观察球与障碍的交互

**预期**：
- 球撞障碍后被阻挡/减速（speed_consume_rate=40）
- 障碍血量逐帧扣除（attack_consume_rate=10）
- 障碍血量归零后消失，球继续飞（或按设计停留）

**判定**：球与障碍有物理交互 = ✅ 通过

---

## 验证6：并行验证面板（任务0的5项）

**⚠️ 重要**：验证6/7 在**测试面板场景**里跑，不是比赛场景。

**操作**：
1. 打开场景 `scenes/test/field_tag_test_panel.tscn`（不是 battle_arena）
2. F6 运行
3. 点击屏幕右下角 **「🔬 并行验证(5组)」** 按钮
4. 结果显示在**屏幕上的日志面板**（不是控制台！控制台只有 `[TagQueue]` `[TagEffect]` 执行日志）

**预期**（5项全绿）：
```
═══════ 并行验证开始 (5组·模拟比赛队列) ═══════
[ 1] ✓PASS 队列窗口(入队不立即执行) | 即时X(应≈X) flush后Y(应>X)
[ 2] ✓PASS 优先级排序(2标签都执行) | atkX→Y defX→Y
[ 3] ✓PASS buff_id唯一(3标签无覆盖) | buff数=3(期望3)
[ 4] ✓PASS 多球员隔离(各自生效) | A:X→Y B:X→Y
[ 5] ✓PASS ball_mods并发累加 | dmgX→Y spdX→Y
═══════ 并行验证完成: 5通过 / 0失败 ═══════
```

**判定**：5/5 PASS = ✅ 通过；若有 FAIL，需先修 handler 再整合

---

## 验证7：原有组合测试不回归（15组）

**操作**：
1. 在**测试面板场景**（不是比赛场景）点击 **"🧪 跑组合测试(15组)"** 按钮
2. 结果在屏幕日志面板显示

**预期**：15/15 PASS（与改动前一致，证明未引入回归）

**判定**：15/15 = ✅ 通过

---

## 整合通过标准

| 验证项 | 必要性 | 通过标准 |
|--------|--------|---------|
| 验证1（启动） | **必须** | 5管理器+6球员+无报错 |
| 验证2（BALL技能） | **必须** | 技能标签执行+球正常发 |
| 验证3（FIELD技能+鼠标守卫） | **必须** | 放置不发球+障碍生成+放置后能发球 |
| 验证4（取消放置） | 建议 | 右键/ESC正常取消 |
| 验证5（障碍耐久） | 建议 | 球障有物理交互 |
| 验证6（并行5项） | **必须** | 5/5 PASS |
| 验证7（回归15组） | **必须** | 15/15 PASS |

**全部必须项通过 = 整合成功**，可以进入下一阶段（扩展更多技能/标签）。

---

## 失败排查指引

| 现象 | 可能原因 | 排查 |
|------|---------|------|
| 启动报 push_back 错 | equipped_skills 类型回退 | 检查 player.gd:76 是 Array[String] |
| 管理器少于5个 | battle_manager 调用缺失 | 检查 _ready 的 _setup_* 调用 |
| 按技能键无反应 | 技能状态机/冷却 | 看控制台 [SkillState] 日志 |
| 放置时仍发球 | 守卫未生效 | 检查 input_manager._any_placer_operating |
| 放置后不能发球 | placer 未退出操作 | 看 operation_finished 是否触发 |
| 并行验证 FAIL | handler 队列 bug | 看 [TagQueue] 日志的执行顺序 |
