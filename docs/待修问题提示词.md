# 待修问题提示词（跨窗口交接用）

> 来源：2026-09-17 技能体系耦合性调研（zcode 窗口）。这些问题**不属于技能体系规划范围**，由主人另开窗口逐个解决。
> **用法**：每个问题的「提示词」可直接复制粘贴给新窗口。⚠ 同一文件相关的两个问题（如 P2-1 与 P0-1 都动 player/trigger）**不要并行开窗口**，串行处理。
> 行号基于 2026-09-17 工作区快照，可能偏移，以实际代码为准。

## 通用验收纪律（每个提示词已内嵌，此处备查）

- 按项目 bug_fix 纪律：先读代码+节点树，人话解释根因；**每轮只修一个根因**，最多改 1-3 文件，不重构无关代码
- 改完必查语法（headless 编译）
- 动了 battle/player/ball/AI 相关文件必须跑 `./run_sim.sh 3 6 1 80` 对比 `sim_results/baseline.json`，**危险总数=0** 才交付
- 完成后写工作日志（工作日志/<日期>.md）

---

## P0-1 疑似：玩家技能双扣能量 + 双CD表

> **【2026-09-20 已修复 ✅】** 静态追踪证实双扣+双表属实（未 runtime print，调用链无守卫已充分）。按方向 A 修复：trigger 为全路径唯一扣费/CD点（含标签附加能耗、施法者消耗/CD倍率），player.use_skill 只做资格前置+CD/显示转发；manual 天赋技能增量同步进 trigger 名册（顺带修好天赋树解锁技能被拒）。验证：test_skill_mults 22/22 + run_sim 危险=0。详见 工作日志/2026-09-20.md。⚠ 残留观察：ai_manager.gd:1279 旧AI仍读 player.skill_cooldowns（恒0，sim 无危险）。

- **危害（人话）**：玩家每放一个技能可能被扣两倍能量，且有两张冷却表各自倒数——AI 只扣一份。若属实，所有技能数值平衡基准都是歪的。
- **证据**：
  - `scripts/battle/player.gd:1577` use_skill 内扣能量、`:1582` 设 player.skill_cooldowns
  - `scripts/battle/battle_manager.gd:1464` 玩家路径转发 `spirit_system.use_skill()`
  - `scripts/systems/spirit_system/spirit_skill_trigger.gd:191` trigger 再查自家 `_skill_cooldowns`、`:214/:332` `_consume_energy` 再扣一次 `p.spirit_energy`
  - 两张 CD 表各自 `_process` 独立倒数；AI 路径只走 trigger（`spirit_ai_manager.gd:1248`）

**提示词**：
> 修复"玩家技能疑似双扣能量+双CD表"问题。背景：技能释放有两条路径——玩家路径 player.use_skill() → battle_manager → spirit_system.use_skill()（trigger），AI 路径只走 trigger。证据显示玩家路径可能在 player.gd:1577/1582 和 spirit_skill_trigger.gd:191/214/332 各扣一次能量、各设一张CD表。第一步先验证：在两处扣能点各加 print（含扣费前后数值），编辑器玩家路径放一次技能，确认 spirit_energy 是否减两次；同时对比 AI 路径。若不属实，报数据收工。若属实，修复方向（二选一，先复述给我确认）：A. player.use_skill 只做资格前置（沉默/解锁检查），扣能与CD全权归 trigger；B. trigger 对玩家路径跳过扣能/CD（由调用方声明来源）。注意保持 AI 路径行为不变、被动技能（纯 trigger 路径）行为不变。验收：玩家/AI/被动三条路径各加断言测试（能量只扣一次、CD 只剩一张表生效），跑 run_sim 危险总数=0。

---

## P0-2 疑似：AOE 范围伤害正式比赛落空

> **【2026-09-20 已修复 ✅】** 静态验证属实（生产仅幻象注册 "players" 组 + AOE 显式跳过幻象 → 命中恒空，逻辑必然，未 runtime print）。修复：ball AOE 索敌改走 `_collect_aoe_targets()`（数据源=`_get_all_players_array()` 即 GameManager.team_a/b 权威名册，与 handler/trigger 注入名册同源）；名册空时兜底回退组（保测试场景）；幻象维持"不吃 AOE"设计现状（过滤条保留为纯防御）。验证：test_aoe_targets 7/7 + 回归三测试 PASS + test_ball_mods_isolation 9/9 + run_sim 危险=0。详见 工作日志/2026-09-20.md。

- **危害（人话）**：球的范围伤害技（AOE）在正式比赛里可能一个真人都炸不到，静默失效不报错。
- **证据**：
  - `scripts/battle/ball.gd:470` AOE 遍历 `get_tree().get_nodes_in_group("players")`
  - 生产代码中注册 "players" 组的只有 `scripts/battle/illusion.gd:94`（幻象冒充球员），真球员不在此组
  - `ball.gd:480-482` 遍历时又显式跳过幻象 → 结果集合为空
  - 对比：handler 用注入的 players 数组索敌（`spirit_tag_effect_handler.gd:611-632`），口径不一致

**提示词**：
> 修复"AOE 范围伤害疑似落空"问题。背景：ball.gd:470 靠 "players" 组找 AOE 目标，但真球员从未注册该组（只有 illusion.gd:94 的幻象注册），且代码又跳过幻象。第一步先验证：grep 全项目 add_to_group("players") 确认注册点；再在 ball AOE 处 print 目标数，跑 run_sim 看是否恒为 0。若不属实，报数据收工。若属实，修复方向（先复述给我确认）：与 handler 的索敌口径统一——优先复用 battle_manager 注入的球员数组（经 battle_manager 转交给 ball，或 ball 经已有引用链获取），弃用组遍历。注意不破坏幻象逻辑（幻象该不该吃 AOE 是设计题，先按现状=不吃，报告里注明）。验收：AOE 命中数断言测试（构造多人场景炸一次，断言真目标受伤），run_sim 危险总数=0。

---

## P1-1 组协议失效三连（隐式依赖静默失效）

> **【2026-09-20 已修复 ✅】** ①"players"组已随 P0-2 闭环（AOE 改权威名册）。②"ball"组恒空：player 新增 `ball_ref` 注入字段（battle_manager 两条初始化路径注入），`_get_ball_node` 优先走注入、组查询留兜底——球技能光环（持球激活技能→球亮光环，取消/球权转移→熄灭）恢复工作。③死信号连接：ball 删除 `_connect_skill_signals/_do_connect_skill_signals/_on_spirit_skill_activated/_on_spirit_skill_cancelled` 及孤儿 `_get_skill_data`（连带删除 P2-2 的一个"每次IO"点）；光环收敛为 player 主动通知单路径，SkillStateManager 信号保留（零订阅者无害）。验证：语法+回归三测试+隔离9/9+AOE7/7+sim 两轮全量6场危险=0。⚠ 排查记录：首轮 sim 曾出现 seed2 偶发极端局（卡死10），stash 对照证明回退三文件反而 seed2 三遍确定性僵死（0-0零击中）——极端局为偶发噪声，非本轮引入。**玩家可感验证**：编辑器进游戏，持球按数字键激活技能→球亮光环；C键取消→熄灭（headless 覆盖不了输入，需主人目检）。

- **危害（人话）**：三处代码靠"组"找人/找球/连信号，但组里根本没注册或注册错了，功能悄悄失效，排查极难。
- **证据**：
  - ①"players"组仅 illusion 注册（见 P0-2）
  - ② "ball" 组只有测试脚本注册 → `player.gd:1733` `_get_ball_node` 生产中恒空
  - ③ `ball.gd:905-922` 连接组 `spirit_system` 节点的 `skill_activated/skill_cancelled` 信号，但组内只有 handler，它没有这两个信号（属于 SkillStateManager，且后者不入组）→ 死连接

**提示词**：
> 修复"组协议失效三连"问题：①"ball"组无人注册导致 player._get_ball_node（player.gd:1733）恒空；②ball.gd:905-922 订阅了组内不存在的信号（死连接）；③（若 P0-2 未处理）"players"组同样问题。第一步：逐条验证（print/断点确认恒空与死连接），列出三条各自的现状表。修复方向（先复述确认）：②直接删除死连接或改连正确对象（确认当初意图——若是想响应技能激活做表现，应连 skill_state_manager 的信号，节点从 battle_manager 注入）；①给 ball 补注册组或改注入引用（推荐后者，与项目"battle_manager 创建+注入"惯例一致）。纪律：每轮只修一个根因，三件事可拆三轮；run_sim 危险总数=0。

---

## P1-2 事件总线：7个零生产者事件 + 关键战斗信号未接线

- **危害（人话）**：广播站有 7 个频道没人播（订阅了永远听不到）；"球砸中人""玩家放技能"等关键事件没接进广播站，被动/天赋类功能扩展被卡。
- **证据**：
  - `scripts/systems/event_bus/event_bus.gd:11-34` 17枚举；`:65-82` wire_sources 只转发 2 条（ball_caught/defeated）
  - 零生产者：BUFF_APPLIED/BUFF_EXPIRED/BUFF_TEAM_EFFECT/STATUS_APPLIED/STATUS_EXPIRED/RESOURCE_ENERGY_THRESHOLD/RESOURCE_STAMINA_THRESHOLD（全项目无 emit）
  - 未入总线：ball.ball_hit_player、ball.ball_out_of_bounds（仅 MatchStats/skill_visual_manager/fx3d 直连）、player.skill_used、field_zone.player_violated

**提示词**：
> 修复"事件总线接线不全"。第一步：盘点核实上列零生产者与未接线信号清单（grep emit/subscribe），标注"该删的枚举（确认无规划用途）vs 该补线的"。修复方向（先复述确认）：①ball_hit_player/ball_out_of_bounds/skill_used 三条接入 wire_sources 或加 emit 点；②7个零生产者枚举逐个裁决——近期能力有规划（buff/状态/能量阈值类被动）的补 emit 点（player.gd add_buff/turn_on_light/能量变动处），无规划的先注释标记"无生产者"防误订阅。纪律：本问题只动 event_bus 与各 emit 点一行级改动，不重构；每个补线点用现有被动/天赋订阅做 headless 验证；run_sim 危险总数=0。

---

## P2-1 球与技能系统的边界（ball 直写 handler 私有字典 + _ball_mods 全局污染）

> **【2026-09-19 第一轮完成 ✅】** 全局污染（按投球者隔离+有效期）与 ball 侧直写/类型直引已修：ball 快照化（私有 `ball_mods`，回旋/追踪飞行状态内联为球私有）、handler `_ball_mods_by_caster` + expires_at（比赛时钟）+ 投球取走即清、trigger 开火只清施法者自己准备区。验收全过：隔离测试 9/9（`scripts/test3d/regression/test_ball_mods_isolation.gd`）+ 回归三测试 PASS + test_skill_mults 22/22 + run_sim 危险=0。详见 `技能规划/04_地基修复计划_ball_mods隔离.md` 执行记录。**残留（第二轮，未做）**：ball.launch 仍经组查找调 handler 快照接口（只读、单一公开方法），彻底解耦（player 侧注入快照）视情况另立项。

- **危害（人话）**：球直接伸手翻改技能系统的内部字典（五处），技能系统改格式球就崩；且修饰符清单是全局一张纸，A 球员开的增益 B 球员投球也吃到。
- **证据**：
  - `scripts/battle/ball.gd:656` 类型直引 SpiritTagEffectHandler；`:740-750` 遍历组抓 handler 实例
  - `ball.gd:271/277/281/291` 直写 `tag_effect_handler._ball_mods` 内部字段；消费点 `:263-291/:463/:505/:717/:967`
  - `_ball_mods` 挂 handler 全局，`trigger.gd:222-223` 每次开火前 reset；duration 字段基本不消费

**提示词**：
> 修复"球与技能系统边界"问题，目标：球侧只读数据、不引用 handler 类型、修饰符按投球者隔离。修复方向（先复述给我确认，可分两轮）：第一轮——handler 提供只读快照接口（如 get_ball_mods_snapshot() 返回 Dictionary 副本），投球时由 player/handler 把快照注入 ball 自己的成员变量，ball 删掉对 SpiritTagEffectHandler 的类型引用与五处直写（改写自己的副本）；顺带解决全局污染（快照按投球者生成，不再共用一张纸）并消费 duration（过期字段失效）。第二轮（如需要）——回旋/追踪等需要"技能侧状态"的字段设计最小回调协议（信号或结果字典），保持球侧无 handler 依赖。验收：A 开增益→B 投球不吃 A 修饰符的断言；ball.gd grep 无 SpiritTagEffectHandler 引用；run_sim 危险总数=0。⚠ 本问题与 P0-1 都动 trigger/player，串行做。

---

## P2-2 技能数据五路并行加载（含两处每次IO）

- **危害（人话）**：五个地方各读各的同一批 JSON，互不通气；其中两处每次用都重新读文件（性能税），热改数据后有的地方更新有的不更新。
- **证据**：DataManager（autoload）已加载 skills/tags_registry/spirits；绕行者：`spirit_skill_trigger.gd:45/:242`、`skill_visual_manager.gd:35-45/:208-223`、`skill_fx_3d_adapter.gd:38-59`、`skill_state_manager.gd:220-240`（每次 _get_skill_data 都开文件）、`ball.gd:934-957`（每次IO）；`DataManager.reload_all()` 不使各私有缓存失效；`player.gd:1780` 调不存在的 DataManager 方法被 has_method 掩盖。

**提示词**：
> 修复"技能数据五路并行加载"。修复方向（先复述确认）：统一走 DataManager——给 DataManager 补技能侧查询接口（get_skill_data/get_tags_registry/缺失的 get_spirit_by_element），五处绕行改为注入或 DataManager 调用；skill_state_manager/ball 的每次IO改为缓存；补 reload_all 缓存失效或明确不支持热重载（文档注明）。顺带修 player.gd:1780 的 has_method 防御（补真方法后删 fallback）。验收：五处 grep 无直接 load json；改 json 后 reload 行为一致；run_sim 危险总数=0。

---

## P2-3 元素颜色表 4 处拷贝

- **危害（人话）**：六种元素的颜色定义抄在 4 个文件里，换色要改 4 处，必漂移。
- **证据**：`spirit_tag_effect_handler.gd:936-946`、`skill_visual_manager.gd:226-236`、`player.gd` `_ELEMENT_COLORS`（:87 附近）、`battle3d/skill_fx_3d_adapter.gd:13-20`（注释自认镜像 player.gd）。

**提示词**：
> 修复"元素颜色表4处拷贝"。修复方向：建单一常量源（可放 battle3d_const.gd 同级或 DataManager 加载 elements.json 的 color 字段——注意 `data/spirits/elements.json` 已有数据，优先数据驱动），4 处改引用。纯常量收敛，低风险；改完 grep 确认无残留色表；headless 编译+技能释放目测颜色不回归即可，run_sim 一轮保险。

---

## P2-4 buff_manager 与 player 内联双实现

- **危害（人话）**：独立的 Buff 模块（105行）没人用，player.gd 里又内联抄了一套同样的逻辑——两处维护必漂移。
- **证据**：`scripts/systems/buff_system/buff_manager.gd`（105行，注释称"不依赖任何战斗节点"）全项目无生产引用；`player.gd:2092-2144` 内联 add_buff/_get_effective_value/_tick_buffs。

**提示词**：
> 裁决并修复"buff 双实现"。第一步先给我裁决材料：两实现的功能差异清单（player 内联版多了什么：duration/tick/属性绑定等）。修复方向（主人裁决后执行，二选一）：A. player 内联为准，buff_system 目录归档/删除（先确认无测试依赖其存在）；B. 反向收敛，风险大不推荐。验收：收敛后六层状态 headless 测试（test_buff_manager/test_skill_mults/test_tick_effects 等）全过，run_sim 危险总数=0。

---

## P2-5 FieldEffectZone 越权直调 player 私有面

- **危害（人话）**：区域效果节点直接调球员的私有函数、直改球员的血量字段——违反项目"副系统出主意，主逻辑动手"的皮影原则，player 内部重构会被它打穿。
- **证据**：`scripts/battle/field_effect_zone.gd:208-243` 直调 `player._get_effective_value`、读写 `player.stamina`、调 `player._on_defeated()`、`is_status_active`。

**提示词**：
> 修复"FieldEffectZone 越权"。修复方向（先复述确认）：按副系统规范（.zcode/skills/subsystem_dev）皮影原则改造——zone 只调 player 公开接口（take_damage/heal 等既有公开方法），私有值读取改用公开查询（若无则 player 补一个公开 getter），defeated 交由 take_damage 路径自然触发。验收：区域烧血/减速/治疗行为回归测试（field_effect_zone 相关既有行为不变），run_sim 危险总数=0。

---

## P2-6 硬编码节点路径

- **危害（人话）**：四处写死 "/root/XXX" 找节点，场景结构一变就静默失联。
- **证据**：`spirit_tag_effect_handler.gd:64` /root/BattleManager（仅测试兜底用）；`player.gd:2164-2172`、`ball.gd:1253-1261` /root/MatchPlayerStats + 父节点 fallback；`player.gd:1364-1367` /root/BattleManager|/root/BattleArena 双路径。

**提示词**：
> 修复"硬编码节点路径"。修复方向：统一改为 battle_manager 创建时注入引用（项目惯例），硬编码仅保留测试场景入口并注释标明"测试专用"。逐处验证生产路径引用非空。低风险清理类，headless 编译+现有测试全过+run_sim 一轮即可。

---

## P2-7 三套索敌口径不一致

- **危害（人话）**：技能找目标用注入名单、球 AOE 用组、幻象靠混组冒充——三处"场上有哪些人"的答案不一样，命中行为不可预期。
- **证据**：handler `:611-632`（注入数组）；ball.gd:470（组）；illusion.gd:94（混组）。

**提示词**：
> 统一索敌口径。前置：P0-2（AOE）完成后做，口径以"battle_manager 注入的球员数组"为唯一权威。修复方向：ball 侧与 handler 侧全部改用注入名单；幻象不再混组，如需被技能/球"看见"，由权威名单显式附带 illusion 标记（或维持现状=不可见，主人裁决）。验收：同一时刻三处获取的目标集合一致断言；幻象行为回归；run_sim 危险总数=0。

---

## P3-1 handler 单文件巨型分发 + 标签映射5处（重构项，最后做）

- **危害（人话）**：所有技能效果挤在一个 1199 行文件里，新增标签要同步 4~7 处；多窗口协作时必然冲突。
- **证据**：`spirit_tag_effect_handler.gd`（1199行/80case/74函数）+ `_init_priority_table`（:70-171，76项）；标签→类别映射另散布 player._TAG_CATEGORIES（:168-174）、input_manager、双视觉层。

**提示词**：
> 重构"技能效果分发器"，**前提：P0-1/P0-2/P2-1 债务清完后再动**。方向（需主人拍板拆分粒度）：按消费路线拆三文件（ball_mods/player_pipeline/field_dispatch）+ 共享工具函数文件，handler 保留 facade 与优先级调度；标签→类别映射收敛为单一注册表（tags_registry 增加字段或 const 字典），五处改引用。铁律：纯等价重构，行为零变化——重构前后各跑一次 run_sim 全量对比（比分/事件账本 diff 应无实质差异），分多轮提交每轮可回退。

---

## 建议处理顺序

```
P0-1（先验证双扣能） → P0-2（AOE） → P1-1 → P1-2 → P2-1 → P2-2~P2-7（可按需穿插，注意 P2-1 与 P0-1 串行） → P3-1（最后）
```

---

## P4 · 存量 sim 卡死偶发排查（2026-09-24 立项）

> **【2026-09-24 已修复 ✅】** 取证 51 场（含全量 stdout 钓局 12 场）：卡死触发 100% 为"外场球员被堵 1s→切镜像位"的单次正常避障（带球卡死 0 次；卡住后事件流连续、全部正常打完；高对抗局发生率最高）——**假设 C（判定口径）坐实，A/B 排除**。修复（两轮迭代）：`ai_manager._report_stuck` 新口径——同一球员 **10s 内同位置**（<40px）反复触发=真僵死才计入卡死硬指标；换位置的再被堵=流动性对抗不计（首版 60s 窗口被 seed2 39s/256 击中团灭局证伪收紧，二版 10s 无位置条件被 seed2/seed7 高干扰局证伪补位判别）。`is_repeat_stuck` 纯函数 5 断言（进 test_wave_op1_patch 40/40；tests/test_ai_utils.gd 为存量损坏 harness——AIManager.new() 本机 -s 恒败、HEAD 同败，与本批无关，上报待修）。**验收：连续 10 场（seed1-10 含历史卡死 seed1/2/3）硬规则 10/10、危险=0、卡死=0**（含一场 244 击中团灭乱战局正确不计）。附带发现（已修 2026-09-25）：run_sim.sh 传球率硬规则阈值 0.8 分数口径与百分数比较恒通过（自方案A落地从未生效，今日 73 场 17 场漏报）——主人批按分级修正：<50% 硬危险 / 50~80% 警告（百分口径），baseline.json 文档同步。详见 工作日志/2026-09-24.md 追加四。

- **现象**：run_sim 偶发"卡死 1-2 次"危险。9 跑 A/B 对照（2026-09-24 实施窗口）：当前树 5 跑中 4 次偶发、HEAD 原状 4 跑中 1 次——**与近期代码批次无关，为存量非确定性偶发**；09-22 起历次日志同模式（偶发、复跑不复现）。
- **危害**：偶发影响自动化回归信任；真僵持若在正式对局出现影响体验。
- **疑似根因方向**（实施窗口预判）：AI 卡死判定阈值 vs 物理时序抖动。
- **涉及文件**（取证后确定）：`scripts/battle/ai_manager.gd`（决策/卡死判定）、`scripts/battle/ball.gd`（球位/交互）、sim 判定脚本。

**提示词（复制给实施/债务窗口）**：
> 排查"存量 sim 卡死偶发"根因。**第一步取证（零代码改动）**：跑 20~30 场 run_sim（多 seed，含已知会卡死的 seed），把卡死局与正常局的 sim_results/eventlog_seed*.json + 比分时间线分别存档；对每个卡死局记录：卡死判定触发时刻、最后 100 帧事件序列、球坐标轨迹、双方 AI 各自最后的决策状态。
> **第二步假设验证（用取证数据，逐假设对照）**：
> A. AI 决策死锁——卡死局双方 AI 是否持续无进攻选技（决策日志）；
> B. 球不可达——球坐标是否长时间静止在某点（卡角落/悬空/被卡）；
> C. 判定误判——卡死期间是否仍有微弱事件流（比赛在进行但事件稀疏被误判）。
> 三假设对应修复方向：A=ai_manager 加僵持打破逻辑（僵持计时超阈值强制进攻行为）；B=ball.gd 加无交互恢复（长时间无碰撞/持有重置重发）；C=校准卡死判定阈值（区分真僵持与低事件期）。
> **纪律**：取证阶段零代码改动；修复阶段一轮只修一个根因，最多 1-3 文件；每轮修复后 run_sim 3 场+此前卡死 seed 复跑对照；拿不准停下来问主人。
> **验收**：修复后连续 10 场 sim 危险=0 且卡死 0（含历史卡死 seed），与基线无其他漂移；回写债务清单与工作日志。

> 优先级：中低（偶发可对照、不影响当前交付；影响自动化回归信任，建议中期排期）。
